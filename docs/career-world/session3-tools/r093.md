### R093 — 2026-09-02 — c1-1 the purple field, first attempt: violet ground is water to the water tools (codex exec via cell.mjs, gpt-5.6-sol, effort=high; director measurement)

- **Setup:** `brief-c1-1.md` from the owner's Outward correction (a saturated magenta-violet carpet, the crystal-crowned knoll, pink-blossom trees, accent shrubs, a closed beck, the line's ground across the field); edit mode with 2,1 (east) and 1,2 (south), both edges dry, the canon as second input, the tone ramp in the target. `224,696` tokens.
- **Verdict: REJECTED by the fringe rings** — `14.35% / 16.67%` against `<1%` — after mask completion had grown the delivered mask by `31,976 px` and filled `1,837 px` of holes. Every other gate green: key-light `0.0018`, rock lighting `0.07`, tone dLuma `7.3` on all land, vegetation `dBG 0.066`, continuity closed.
- **The defect is the gate's, and the growth's, not the mask's** (`fringe-hue.mjs`, on the DELIVERED mask): the gate's water test (`mx>40, sat>.25, b>r, b>=g`) flags `29.6% / 24.1%` of the ring pixels; of the `44,700` flagged, `42,237` have hue `270–300` and `2,325` hue `240–270` — the violet ground. With the same test limited to hue `150–225` (cyan to blue) the rings read `0.12% / 0.03%`, a pass; on the accepted cells the two tests agree (1,3 `0/0`, 4,2 `0.09/0.06`, 3,1's pre-growth `5.6/2.8` → `5.6/2.6` — real blue water). The growth uses the same test, so it ate `32k px` of purple around the beck. **Lock change 7 proposed:** one shared `isWaterPaint` with the hue window, used by both rings and the growth; control: a violet annulus beside a water disc is neither fringe nor grown into, while the blue-ring control still fails.
- **Eyes** (`review/regen-c1-1-cand1-quarter.png`): the field is the owner's place — a saturated magenta-violet carpet, the crystal knoll ringed by rock, blossom trees, gold and blue shrubs, a teal beck to two pools. Two things to change regardless of the gate: a conspicuous bare-soil PATH across the field (the drawn-line problem of R092's 4,0 again — my brief said "the line's ground… gentle open ground", the model drew a track) and the purple running right to the seams against the moor: the tone ramp carried the moor's olive into the target and the brief still said the field fills everything, so the model painted purple to the line. The luma gate cannot see a hue step (dLuma `7.3` while the hue flips from olive to violet) — a chroma term in the tone gate is the candidate 7b, the owner's eye the check today. The knoll landed north-east of centre; crowns `44 px`.
- **Attempt 2** (`brief-c1-1-r2.md`, after lock change 7): no path or line of any kind; the east and south thirds painted as the moor the target shows, the purple beginning past them with its clear edge; the knoll north-west; the crown floor.
- **Evidence:** `.codex-tmp/session3/regen-c1-1.log`, candidate at `.codex-tmp/session3/rejected/c1-1-cand1/`, `fringe-hue.mjs` output above.


### R091 — 2026-09-02 — c1-3 accepted on the fourth attempt: the landform took the rock away, and the first bake with the tone ramp put the transition inside the range (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** `brief-c1-3-r4.md` — the layout of candidates 2–3 kept (the bright shelf, the beck from the north to a pool, one fall into an inlet, the sound edge to edge), the rock removed as a class: no column stacks, terraces or slab pavements; shingle and sand shores, grassy slopes over rounded moss-covered rock, the sound over sand; the crown floor; and, new with lock change 6, the edit target carrying the neighbours' olive fading inward across the north and east thirds. `190,579` tokens.
- **Verdict: ACCEPTED on all gates** — rock lighting `0.088` on only `2,107` strong rock edges (report-only; the three rock-heavy candidates: `0.288 / 0.187 / 0.198` on 11–24k edges): the habit was never cured, the rock that carried it was taken away. Key-light `0.0057`; continuity ok with 2 crossings (1,2's beck continued); fringe `0/0%`; land `73.6%`; `140` tiles. Committed `b16375b`. **ELEVEN CELLS STAND.**
- **The ramp's first result** (`tone-seam.mjs` on the stitched world): the north seam against 1,2 reads dLuma **`15.0`** where candidate 3 read `24.5` — inside the accepted range (`2.3–17.4`) on the first bake with the ramp; the east seam against 2,3 reads `1.0`. The gate row printed both: "no vegetated seams; tone worst dLuma 15.0 on all land over 2 seam(s)". Chroma still shifts across the north line (d(b−g) `19`, yellow-green meadow against olive moor) — the owner's eye on the stitched seam decides whether luma alone is the right measure or the ramp should carry chroma weight too.
- **Worker-reported misses for the owner's eye:** the gully below the fall is dry pale sand before it reaches the sound rather than a drowned inlet (the rail's water crossing becomes a sandy gully crossing unless a later pass floods it); crowns `60 px`; some rounded boulders keep lighter tops.
- **Model-attributable:** two levers landed in one bake and both measured: a landform stated as an absence of a rock type ("no columns, terraces or pavements") changed the rock-only moment where three briefs of shading words and a rock reference image had not; and a transition given as pixels moved a seam from `24.5` to `15.0` where the Transitions text had not.
- **Evidence:** `.codex-tmp/session3/regen-c1-3.log` (after `rebake4-start`), `review/stitched-c1-3-*`, `review/territory-grid-status.png` (11 of 20).


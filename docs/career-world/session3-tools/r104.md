### R104 — 2026-09-02 — c1-0 the bare plateau with the crater tarn, second attempt accepted; eighteen cells stand (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** `brief-c1-0-r2.md` — the lit rock named with its number and the neighbouring plateau's passing number, "one flat value per face" verbatim, bench edges that wander, the altar as weather-made rock. `171,391` tokens.
- **Verdict: ACCEPTED on all gates** — rock lighting `0.138` (attempt 1: `0.171`), key-light `0.0018`, seam tone `0.7` on all land and vegetation `dBG 0.081` against 0,0, fringe `0.07% / 0.02%`, closed water with the round tarn and its complete rim, land `98.1%`, `131` tiles. Committed `dccab43`. **EIGHTEEN CELLS STAND.**
- **Worker-reported for the owner's eye:** long regular bench runs with square-ish turns and a block-like altar remain (the worker's own visual checks false); crowns `51 px`. The rock gate is a number; the palisade rule is the owner's eye, as everywhere.
- **Model-attributable:** naming the measured failure with the neighbour's passing number moved the rock moment on the first re-bake (`0.171 → 0.138`), as it did on 3,1 (`0.31 → 0.143`) and did not on 1,3 (a rock-heavy sound shore) — the lever works where the rock is benches and columns, not where it is a whole shore of them.
- **World state:** 18 of 20; the two remaining wait on the owner — 1,1 the purple field (lock change 7, the hue window on the water test) and 2,0 the dark-forest gorge (parked after three lighting strikes: accept by eye / fourth attempt / leave).
- **Evidence:** `.codex-tmp/session3/regen-c1-0.log` (after `rebake2-start`), `review/stitched-c1-0-cell-half.png`, `review/territory-grid-status.png` (18 of 20), `review/territory-L2-quarter.png`.


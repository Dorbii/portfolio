### R108 — 2026-09-02 — lock change 9: content-aware seams (owner: "if you think its needed go ahead" / "min cut is fine here"), and the world restitched

- **Setup:** the owner's strip of the 2,0 candidate-4 west seam against 1,0 ("trees and other features are legit cut in half"), located by cross-correlation (r `0.98`) in the quarter preview. The preview draws a straight cut; the pipeline's real seam (corner jitter 64, wiggle 64, feather 8; ported to `.codex-tmp/session3/seam-geo.mjs` and checked against the stitched tiles) cuts the same stubs and conifers, because a fixed line slices whatever straddles it and no worker continues a neighbour's features to the pixel. Hard-edge excess at the real seam (`seam-edge2.mjs`, the paint's own rate as the control) did NOT single the flagged seam out (`x0.88` against `x0.70-1.88` on accepted seams): busy jointed rock hides cut features from a luma-step rate. Diagnostic only.
- **The change (cell.mjs, approval recorded before and after):** between two AUTHORED cells the boundary is the minimum-error path through the two l2 paints (image quilting's cut), within `+-128` px of the nominal line, `+-1` px per step with a step penalty of 2, pinned to the jittered corners by the wiggle's own taper envelope so perpendicular seams agree at every corner, derived from the two sources alone so both cells compute the same boundary; edges to unauthored ground keep the wiggle; feather, water cut, gates and edit targets unchanged; a water-cut mismatch costs 255 so the cut never crosses one. `--restitch` rebuilds an accepted cell's tiles from its recorded sources with no dispatch and no gates. The manifest records the seam contract. Prototype numbers (mean paint disagreement along the boundary, wiggle -> cut): flagged seam `46.6 -> 12.8`; 2,0|3,0 `49.5 -> 13.9`; 0,0|1,0 `36.5 -> 11.6`; 2,1|3,1 `35.4 -> 9.1`; 1,2|2,2 `26.9 -> 8.6`; the cut within 125 px of the nominal line.
- **Controls (suite, written first):** a dark crown painted on ONE cell's canvas, centred on the boundary the pipeline itself drew against the plain neighbour (read from its binding map), must come through whole or not at all with no blend inside it; `--restitch` byte-idempotent and refusing an unauthored cell; the manifest contract. On the pipeline as it was: the crown cut, `44.7%` of it showing with blends at the line (standalone reproduction, `crown-repro.mjs` against the pre-change script). With the change: the crown absent, every pixel opaque, no blend. A first placement of the control on cells 1,2/2,2 collided with the stream controls' cells (an already-authored cell exits 0 untouched), which is why the control now asserts its own stitches happened. Suite: SUITE_RESULT.
- **Restitch:** RESTITCH_RESULT.
- **Evidence:** `.codex-tmp/session3/review/c2-0-cand4-west-seam-compare4.png` (owner / straight / real seam / min-cut), `regen-c2-0-cand4-real-seam-W-north.png` vs `c2-0-cand4-mincut-W-north.png` at 1:1, `seams-before-{vertical,horizontal}.png` and `seams-after-*.png` at every seam, suite logs `suite-lock9-{before,after3}.log`, `restitch-world.log`, `tools/world-authoring/solidified.json` history.


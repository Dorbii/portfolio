### R094 — 2026-09-02 — c4-0 Kaizen REPLACED on the open-ground brief: the road is gone (owner "this looks weird"; codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Owner ruling (crops of the stitched 4,0):** "this looks weird" — the station strip as a pale graded band with hard straight edges, a straight cut running off it, short walls beside it. **Cause:** my brief asked for a "treeless strip three crowns wide" and a "straight cutting" — the model drew them as built geometry. Rule recorded (STATE cautions, lock-change 5k): the land offers OPEN ground and NATURAL draws, never strips, bands, cuttings or corridors described as lines; the rail and structures layers draw those. The owner keeps 2,1's softer strip ("the city covers it").
- **Setup:** `--force` bake on `brief-c4-0-r2.md` — the shelf's south side simply wide, level and open with soft edges and unbroken texture; the tunnel approach a natural shallow draw with rounded sides; no walls inland; "nothing that reads as built". `144,401` tokens.
- **Verdict: ACCEPTED on all gates** — key-light `0.0015`, rock lighting `0.084`, seam tone dLuma `1.5` on all land against 4,1, fringe `0/0%`, land `88.8%`, `123` tiles; the worker's own check `previous_road_or_station_strip_defect_absent: true`. Committed `e081f82`. Twelve cells stand (replaced; the count is unchanged).
- **Still flagged by the worker:** crowns `49 px`; some cliff runs regular; the sea reaching 93 px of the north edge at the corner (the coast continuing — correct in the paint, too literal in the brief again).
- **Evidence:** `.codex-tmp/session3/regen-c4-0.log` (after `replace-start`), `review/stitched-c4-0-r2-cell-half.png`.


### R090 — 2026-09-02 — Lock change 6: the seam gate sees all land, and the transition is given as pixels (owner: "lock that down… go with that change"; controls first)

- **Owner ruling:** on 1,3 candidate 3's north seam, "The seams are stark here… Need a better transition", "the assets on that border are fine though" — a tone step on the ground, not a structural clash — then "oh got it yeah lock that down… go with that change".
- **(6a) The palette gate on all land.** Beside the vegetation-median check (which reported "no vegetated seams" on a moor and passed the stark step, R088), the gate now takes the median luma of ALL opaque land in the 16..176 px bands on both sides of every authored orthogonal seam and fails above `20`: the stark seam read `24.5`, every accepted seam `2.3–17.4` (the owner-accepted quarry seams at the top). The gate row reports both the vegetation and the all-land numbers; a seam with no vegetation on one side is no longer a pass by absence.
- **(6b) A tone ramp in the edit target.** The Transitions text asked for the biome change inside the outer third; the model changed tone at the line. Now, for each authored orthogonal edge, per 64 px segment along it, the median colour of the neighbour's last 64 px of bleed paint before the grey is blended into the grey from full strength where the grey begins (256 px inside the kept edge, where the neighbour's bleed ends) to flat grey a third of the kept area in from the seam. Only grey pixels change; painted pixels never do. The model continues a tone it can see.
- **Controls:** (6a) a synthetic cell with a +30 luma lift on every channel in its outer third fails with `tone dLuma … on all land` and leaves the world untouched, while the unlifted pair passes; (6b) the dry-run target for an edit-mode cell reads the ramp at two depths inside the third (within ±2 per channel of the blend of the flat grey and the neighbour's band median) and exact grey beyond it. **Director errors on the way, caught by the controls:** the first ramp started at a quarter strength beyond the neighbour's bleed; the "fix" sampled the grey instead of the bleed (the frame's v = 0 is the kept edge, the bleed is v < 256); and two suite runs were started on the shared test world a few minutes apart and wrecked each other (`ENOTEMPTY`, then missing sources) — a suite-level lock is queued (6d).
- **Suite:** SUITE_RESULT. Lock history has the before and after entries; hash gate intact; commit COMMIT_HASH.
- **First use:** 1,3's fourth attempt on the low-rock landform (`brief-c1-3-r4.md`) — the first bake whose target carries the ramp; its seams measured on all land before anything else.


### R088 — 2026-09-02 — The stark seam the palette gate could not see: vegetation-only medians skip a moor, and a tone change at the line passes (owner's eye on 1,3 candidate 3; director measurement)

- **Owner:** on the 1,3 candidate's quarter view — "The seams are stark here, assuming because its a new biome. Need a better transition."
- **Why the gate passed it:** the palette gate compares vegetation medians (`g > r && g > b && g > 60`) in the 16..176 px bands; the moor's olive has `r >= g`, so both bands had under 500 "vegetation" pixels and the gate reported "no vegetated seams" — a pass by absence. The step the owner sees is a tone step on all ground.
- **Measured on ALL opaque land** (`tone-seam.mjs`: band medians of luma, r−g, b−g): the stark seam (1,3 candidate 3 north edge vs 1,2's south band) reads **dLuma `24.5`**, d(b−g) `16`; its east seam vs 2,3 reads `10.7 / 5`. Every accepted seam in the world reads dLuma `2.3–17.4`: 1,2|2,2 `3.2`, 2,2|3,2 `12.7`, 2,2|2,3 `2.3`, 2,3|3,3 `8.8`, 3,3|4,3 `17.4`, 3,2|3,3 `7.2`, 3,1|3,2 `7.0`, 2,1|2,2 `4.9`, 2,1|3,1 `3.3`, 4,2|4,3 `14.0`, 3,2|4,2 `4.5` — the top two are the quarry's, which the owner accepted by eye. A limit of `20` separates the stark seam from every accepted one.
- **The second half of the problem:** the brief said the change to the brighter shelf happens inside the cell across its outer third, the packet's Transitions section says the same, and the model brightened to the line anyway. Words do not place a transition; pixels do — the same lesson as the edit target (R057) and the canon as second input (R078). Proposed: blend the neighbour's edge-band tone into the target's grey across the outer third so the model continues a tone it can see (lock change 6b), and gate all-land dLuma at `20` (6a). The seam-step gate (5c) remains the finer instrument for structure.
- **Evidence:** `tone-seam.mjs` output above; `review/regen-c1-3-cand3-quarter.png` (the owner's crop is its north-east quarter).


### R081 — 2026-09-02 — c1-2 heather moor, third attempt: the canon as the second input replicates on a real bake; eight cells stand; the pyramid checked offline (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** `brief-c1-2-r2.md` unchanged from attempt 2; the only difference is lock change 5 part 2 — the packet mandated the canon source as the second input of the single edit call with the reference paragraph. The worker confirms `mode: "built-in imagegen edit with edit target and canon reference"`, one call, no retry. `273,524` tokens.
- **Verdict: ACCEPTED on all ten gates** — key-light `0.0068` against `0.011` (attempts 1 and 2 on the same edit target: `0.021`, `0.0186`; the probe R078: `0.0093`); rock lighting `0.337` on `1,309` strong edges (report-only); fringe `0% / 0%`; mask completion `+0 px` (the delivered mask was complete); continuity ok, closed as briefed; palette "no vegetated seams" (moor). `167` tiles. Committed `ee81a13`. **EIGHT CELLS STAND.** Director measure on the stitched l2: `0.0056 @ 33°` — the direction is still the inherited one, at a quarter of the strength.
- **Eyes** (`review/stitched-c1-2-quarter.png`, `stitched-c1-2-tors-1to1.png`): the hamlet clearing ten crowns wide beside the beck, the lookout crag on the south side as the tallest rock with a flat top, tors scattered and broken; the tussock emboss faint at 1:1; the beck long with more pools than the brief's two or three (worker-reported). Worker-reported crowns `49 px` final against the `84` floor — the crown drift persists and is the next thing to anchor (lock change 5e report-only first).
- **Model-attributable:** the hand travels through the images the model is given; giving it the canon beside the neighbour halves the inherited light in one shot, on a probe and on a real bake. The mechanism, not the words, was the lever (R075/R076).
- **Offline LoD check (owner asked whether the pyramid works before more cells are spent):** `pyramid-sheet.mjs` → `review/pyramid-A-block-L3-L6.png` (the 7-cell block at L3, L4, L5, L6 native) and `pyramid-B2-seam-stretched.png` (one 512 px window with the 3,3|4,3 seam down its centre at L0 native and L1–L3 stretched back with nearest sampling): the seam is not findable at any level, the brush character survives to L3, the tone holds across tiers. The reduction arithmetic is the suite's control. **Untested:** the runtime — the app has never streamed this pyramid; its land streamer takes a manifest of tiles with normalized world bounds and two source tiers (capital, site) across five camera tiers with fixed cross-fade spans. Wiring the pyramid in for a stripped dev preview (manifest generator, a streamer extension for more tiers, a layer-only toggle, a dev placement from NinjaOne's focus view) is app work, about half a day, awaiting the owner's word.
- **Grid:** `review/territory-grid-status.png` — 8 of 20.


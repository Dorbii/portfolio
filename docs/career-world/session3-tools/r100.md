### R100 — 2026-09-02 — c2-0, third attempt: the reframe fixed the seams and the composition; the lighting stayed over; parked after three strikes (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** `brief-c2-0-r3.md` — both outer thirds as the neighbours' ground at their brightness, the forest in the core, a gorge of rounded mossy rock, conifers the same value all round, one water system. `156,120` tokens.
- **Verdict: REJECTED by the two lighting gates only** — key-light `0.0117` (limit `0.011`) and rock lighting `0.189` (limit `0.15`). Everything the reframe targeted landed: seam tone `5.7` on all land over both seams (from `20.0` and `20.6`), vegetation `dBG 0.035`, one closed water system, the mill ledge unmistakable, crowns `105 px`, the worker's own checks "south moor brightness held" and "east plateau brightness held" both true.
- **Hypothesis tested and rejected:** that the rock gate was measuring the near-black forest floor (dark ground is "rock" to the classifier). The strong rock edges are all on bright rock (`13,161` at luma ≥ 40, none below); the block map puts the moment in the east third (`0.2–0.33` toward ~265°, thousands of edges per block) — the pale plateau benches painted to match 3,0, shaded with lit tops, plus the mill-ledge face. The rock is real and lit; the gate is right.
- **Eyes** (`review/regen-c2-0-cand3-quarter.png`, `regen-c2-0-cand3-east-1to1.png`): the best of the three by far — the forest opening onto moor to the south and pale plateau to the east, the gorge stream with two falls, the round level ledge beside the plunge pool. At 1:1 the east benches' jointed tops and the column stubs carry a mild lit side; the conifers a faint lit flank.
- **Parked after three strikes — the owner's call:** (a) accept candidate 3 by eye with a recorded exception (`0.0117 / 0.189` — the rock number is not marginal, the look is mild); (b) a fourth attempt with the east third's benches explicitly flat-shaded ("every bench top and column face one value, jointed by thin lines only"), which is the one instruction the third brief did not carry for that third; (c) leave it parked. Candidates 1–3 at `.codex-tmp/session3/rejected/c2-0-cand{1,2,3}/`.
- **Evidence:** `.codex-tmp/session3/regen-c2-0.log` (after `rebake3-start`), `rocklight-map.mjs` output in the session transcript.


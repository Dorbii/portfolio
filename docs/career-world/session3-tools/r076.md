### R076 — 2026-09-02 — c1-2 heather moor, second attempt: concrete words do not move an inherited key light (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** `brief-c1-2-r2.md` — the emboss named exactly (no bright upper edge, no dark lower edge on any tussock; boulders the same value on every side; both banks the same value), size anchors in crowns, crowns tied to the LARGEST arriving ones. One capacity refusal retried by the runner (120 s). `210,514` tokens, one edit call at 1254 px.
- **Verdict: REJECTED by the key-light gate again** — `0.0186` against `0.011` (attempt 1: `0.021`); `keylight-map.mjs`: global `0.0188 @ 36°` (attempt 1: `0.0205 @ 33°`). Every other gate green. The worker's own proxy passed again (`aggregate_directional_key_proxy: true`) and it reported `equal_value_on_all_terrain_faces: false` itself.
- **The experiment's answer:** the words that moved the rock-only moment on 3,1 (`0.31 → 0.143`, a column-shading habit) moved the global moment here by a tenth and left the direction untouched. The emboss is the arriving neighbour's hand (2,2 at `0.0121 @ 34°`), continued and amplified; in edit mode the edit target is the only image the model sees, so no prompt reaches past it. Eyes at 1:1 (`review/regen-c1-2-cand2-tors-1to1.png`): the same tussock emboss, and the "low outcrop sheltering the clearing" came out as a regular fence of identical columns with a square corner — the owner's rejected geology, again.
- **What improved:** crowns `69 px` final (attempt 1: `45`) once the brief tied them to the largest arriving crowns; the clearing width and the lookout crag landed by the worker's report.
- **Two strikes → no third bake under the same conditions.** The lever is a pipeline anchor (lock change 5h): (i) the canon as a second input image in the same edit call, if the built-in tool accepts two images — a packet-text change only; (ii) failing that, the canon's paint ghosted at low opacity into the target's grey "to paint" area so the hand the model continues is the canon's in the interior and the neighbour's at the band. Either needs one owner-authorised probe run outside the pipeline (as R056/R057 were), measuring the moment and the seam fidelity (`r` against the neighbour's band; the pipeline's own path measured `0.74`).
- **Evidence:** `.codex-tmp/session3/regen-c1-2.log` (after `rebake2-start`), candidate at `.codex-tmp/session3/rejected/c1-2-cand2/`, `review/regen-c1-2-cand2-quarter.png`.


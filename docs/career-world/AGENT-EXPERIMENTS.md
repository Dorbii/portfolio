# Agent Flow Experiments — Career World

Maintained by Claude (director). One entry per dispatched run (desktop thread task or `codex exec` dispatch). Purpose: build a dataset of which model tiers, effort levels, and packet shapes each pipeline stage actually needs, so later cities get cheaper and smoother.

## Summary table

| Run | Date | Task | Lane | Model | Effort | Wall time | Outcome | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R000 | 2026-08-21 | I24 checkpoint + T1 grammar r1 | desktop thread | (Steve: fill in — app default) | default | ~28 min | **partial** (r1 rejected, machinery approved) | fresh-session baseline under v2 contract |
| R001 | 2026-08-21 | T1-r2 grammar revision | codex exec | gpt-5.6-terra | medium | — | dispatched | Terra probe on a mechanical packet with explicit criteria |

## Run log

### R000 — 2026-08-21 — I24 old-lane checkpoint + T1 grammar extraction (desktop thread)

- **Setup:** fresh desktop session (id `01a02544-…adf9c`), cold start on AGENTS.md + QA-REVIEW.md. Model = app default (Steve to record which tier the desktop app is set to).
- **Observations so far:**
  - Correctly ingested both steering docs on first turn; explicitly cited them as authority and scoped itself to "I24 checkpoint plus one active T1 slice" — the fresh-context reset worked; no old-lane vocabulary reappeared.
  - Self-selected local skills (`plan-evidence-verification`, `task-packet-execution`, `gate-discipline`). Watch item: Steve suspects some local skills are stale; note any skill-driven weirdness here.
  - Chose to run the **full** gate suite before committing a checkpoint of already-finished work — good attribution discipline, but ~5 min of wall time dominated by the 145 s exhaustive camera-sweep test. Flow finding: the packet should state which gate tier a checkpoint needs (focused vs full) so workers don't overspend on ceremony.
- **Verdict:** PARTIAL. I24 checkpoint (`ca6ebde`) clean and well-scoped. T1 r1 delivered working deterministic machinery, reproducible outputs, and correctly-shaped property checks in ~20 min — but fidelity failed review: landmarks missed (palace, train hall, observatory), 45 uniform stamps vs ~150+ structures, sparse circulation (east ridge/industrial empty), abstract bridge diagonals over rooftops. Model-attributable read: the worker satisfied every *written* criterion and stopped at the minimum viable interpretation of "faithful" — the packet under-specified fidelity, so this is as much a packet-authoring finding as a model finding. F3 logged.
- **Wall time:** ~28 min total (~5 min of it full-suite gate ceremony on the checkpoint — see F1).

### R001 — 2026-08-21 — T1-r2 grammar revision (codex exec, gpt-5.6-terra, effort=medium)

- **Setup:** first headless dispatch. Packet `.codex-tmp/qa/T1/packet-t1-r2.md` with explicit numeric fidelity criteria (≥100 footprints, structure-dominance validation, ≥70% on-path polyline samples, landmark class, bridge-span rule). Sandbox workspace-write; no-commit; focused gates only.
- **Hypothesis being tested:** a mid-tier model with a *fully specified* mechanical packet matches flagship output quality on extraction/scripting work.
- **False start 1:** dispatch died at launch — stale PATH CLI (see F4). Redispatched 12:40 on the app's v0.149-alpha engine.
- **False start 2:** worker blocked — sandbox denied Node execution; honest blocker report, no commit, clean stop (left an unverified builder draft).
- **False start 3:** PATH-preamble fix insufficient — execpolicy denies system Node script execution outright (probe matrix + rules-file archaeology pinned it down; see F5). Worker again reported honestly and claimed nothing. Its blind draft crashes at `buildFootprints` — noted in the packet for the next attempt.
- **Attempt 4 (first fair attempt, ~13:25):** dispatched with bundled-node instructions (F5 unlock) and the known draft crash called out.
- **Verdict: STRUCTURED FAIL — high-quality failure.** Terra fixed the draft crash (`structureFootprint` missing return), ran builder + check + lint, and correctly diagnosed that the *approach* was invalid: splitting the registered abstract routes cannot satisfy the ≥70% on-path property (worst edge 18.75%) because layout anchors are not street geometry. It invoked the two-strikes rule, refused to weaken the classifier, requested a director reframe, claimed nothing, committed nothing. Model-attributable: at medium effort Terra executed a debug-run-validate loop competently and — notably — recognized a dead-end approach rather than thrashing. The dead-end itself was packet-induced (see F6), not model-chosen.
- **Cross-ref:** superseded by R001b (r3 pixel-extraction reframe, Terra high effort).

### R001b — 2026-08-21 — T1-r3 grammar via pixel extraction (codex exec, gpt-5.6-terra, effort=high)

- **Setup:** director reframe per Terra's request: circulation extracted by pixel classification → skeletonization → vectorization (on-path property true by construction); layout manifest demoted to naming/district priors; director-supplied landmark seed points; per-district self-normalizing coverage property. Effort bumped medium→high for the vectorization work. Strike counter reset — new approach.
- **Verdict: PARTIAL — converging (Terra strike 1 on this approach).** Real progress: implemented Zhang-Suen skeletonization + vectorization competently, classifier traces actual streets, all districts covered, all three landmark seeds hit, honest flag that calibration needs director judgment. Two visual failures the properties didn't catch: (a) 542 *disconnected* dashes — no connectivity property was specified, so no network topology was built; (b) footprint classifier keyed on warm masonry and missed the dominant dark-slate roofs → fragments instead of building extents; palace captured west-wing only. Also: "minimum interior rate 0.7000" landing exactly on the 0.70 threshold reads as tuned-to-pass — properties held while visual truth didn't, confirming the director-eyeball gate is non-negotiable.
- **Model-attributable:** Terra at high effort handled a nontrivial CV pipeline (skeletonization, pruning, vectorization) without hand-holding. Failures trace to packet omissions (connectivity, roof material) more than model capability. Packet-authoring lesson → F7.
- **Cross-ref:** R001c (r4 refinement packet: topology joining + connectivity property, roof-material class + component merging, director-supplied landmark bboxes with ≥70% overlap property, overlay readability fix).

### R001c — 2026-08-21 — T1-r4 grammar refinement (codex exec, gpt-5.6-terra, effort=high)

- **Setup:** refines r3 in place. Dispatched ~13:20.
- **Verdict: CORRECT STOP — director spec error.** Terra implemented everything asked (gap linker, junctions, roof classification, merging, landmark properties, readable grid), then proved the ≥60% visible-pixel connectivity property physically unsatisfiable (164 components / 0.053 share; classifier widening → 1,254 / 0.008) and stopped under two-strikes without weakening properties or faking artifacts. It even flagged that the stale r3 PNGs were diagnostic-only rather than presenting them as passing output. Landmark diagnostics: palace 0.916, observatory 0.793, train hall 0.679 vs my generous bbox.
- **Model-attributable:** third consecutive honest stop; at high effort Terra now reliably distinguishes "I failed" from "the spec is wrong" — the exact judgment quality the tier question was probing. Director takeaway → F8.
- **Cross-ref:** R001d (r5: occlusion-aware inferred links with anti-abuse bounds; train-hall tolerance corrected).

### R001d — 2026-08-21 — T1-r5 occlusion-aware topology (codex exec, gpt-5.6-terra, effort=high)

- **Setup:** corrected topology model — labeled inferred links across occluders (≤70 px, heading-continuous, structure/foliage crossings only, water = bridge only), largest component ≥60% incl. inferred, inferred ≤40%, components ≤40. Dispatched ~13:42.
- **Verdict: CORRECT STOP — second director spec failure.** Implemented the inferred-link mechanism with two distinct tunings as instructed; both yield 38 links, 126 components, 7.8% largest share vs the 60% bar. Refused to regenerate artifacts from a red build. Train-hall tightened bbox passes (0.7583) — landmarks settled. Two blind threshold corrections in a row failing is a director process smell → F9 (measure before spec'ing).
- **Cross-ref:** R001e (r6 diagnostics: topology gate demoted to metrics, component-colored overlay, length histogram; final property to be set from data).

### R001e — 2026-08-21 — T1-r6 topology diagnostics (codex exec, gpt-5.6-terra, effort=high)

- **Setup:** measure-then-spec. Expected to run green (topology reported, not asserted) and regenerate all artifacts. Dispatched ~14:08.
- **Verdict: PASS — and it closed T1.** Green build, full diagnostic block (126 components; ≥50 px components hold 61.8% of length; <40 px hold 31.9%; p50 30 px; inferred 23.6%), component-colored overlay, 151 footprints, landmarks all passing. The measured distribution let the director set satisfiable bars (≥60% in ≥50 px components; ≤35% in <40 px) that current output passes → **T1 DIRECTOR-ACCEPTED as grammar v1** with recorded limitations (short occluded runs, partial mid-building fragmentation) to be revisited only on demonstrated composer need.
- **Terra tier summary across T1 (the first real experiment result):** 7 dispatches, zero dishonest claims, three correct two-strikes stops that each disproved a director spec, competent CV implementation (skeletonization, occlusion linking, component analysis) at high effort. Preliminary answer to the tier question: **Terra + rigorous packets + director QA is sufficient for mechanical pipeline stages**; every failure in T1 traced to spec/packet authoring, not model capability.

### R003 — 2026-08-21 — T1 finalize + checkpoint (codex exec, gpt-5.6-terra, effort=medium)

- **Setup:** promote measured bars to enforced properties; narrow-path checkpoint commit (the only commit-authorized packet). Dispatched ~14:25. R002 (T2, Sol) queues behind it — one writer at a time.
- **Verdict: PASS with one infra discovery.** Properties promoted and green (0.6177/0.3186 vs 0.60/0.35 bars), ESLint clean, honest QA note — but the sandbox denies `.git` writes entirely (`index.lock` cannot be created), so workers can never commit (F10). Director verified the check in own shell (exit 0), inspected the package.json diff (only the two grammar script lines), staged the exact authorized path list, and created checkpoint `8fd8894`. New standing protocol: **workers finalize, director commits.**

### R002 — 2026-08-21 — T2 kit-of-parts contract (codex exec, gpt-5.6-sol, effort=high)

- **Setup:** first flagship-tier dispatch; design-shaped task. Packet `.codex-tmp/qa/T2/packet-t2.md`: kit taxonomy, per-class generation contract template, exact socket/baseline standard, generation workflow rules, minimal D05 proof-slice subset, open questions for Steve. Deliverable `docs/career-world/CITY-KIT-CONTRACT.md`. Dispatched ~14:40.
- **A/B context:** Sol/high on design work vs Terra/high on mechanical work — the two lanes of the tier hypothesis now both have live data streams.
- **Verdict: PASS at director review (~25 min wall).** 250-line contract; taxonomy with measurable class boundaries (px² ranges from the grammar), a genuinely precise socket/baseline standard (baselineY/sortPoint/footprint-vs-silhouette/terrace modes/support polygons), reuse-first foliage (0 new entries), comprehensive mechanical gates, minimal D05 slice (26 entries), and four well-chosen owner questions. Every numeric citation checked out against the grammar JSON — including one (595 edges) that corrected the director's own stale memory of interim counts. One gap noted for per-asset packets (scale-coherence gate).
- **Sol-vs-Terra observation:** on a design-shaped task Sol produced near-acceptance-quality work in one pass with zero revisions — versus Terra's honest-but-iterative convergence on mechanical work. Early but consistent with the tier split: Sol where the task is open-ended synthesis, Terra where properties can bound it. **Awaiting Steve's acceptance.**

### R004 — 2026-08-21 — T3a composer skeleton, D05 placeholders (codex exec, gpt-5.6-terra, effort=high)

- **Setup:** T2 accepted by Steve (parameters in CITY-KIT-CONTRACT.md §9; commit `e779ffe`). T3 staged: skeleton (a) → kit generation (b) → full composition (c). T3a validates placement/sockets/consolidation/z-sort with procedural placeholder parts — zero generation spend at risk. The composer is also the grammar's first real consumer; its friction report drives any grammar revision. Dispatched ~15:20.
- **Verdict: MIXED — mechanics PASS, registration proof unusable, friction report excellent.** 28 road + 9 stair runs, 25 placements, 0 z-sort violations, deterministic, green gates. But the registration proof rendered terrace bands as opaque full-width horizontal strips, hiding the master entirely — one revision (R005). The friction report earned its keep: named anchors displaced 146–300 px from nearest grammar footprints (S01/S18/S11), D05 band data is left-edge samples only, 25 footprints thin vs the visibly dense district. **The skeleton stage caught grammar-v1's gaps before a dollar of generation spend — exactly why T3 was staged.**
- **Director decisions:** registered anchors become placement authority for named entries; terrace bands demoted to advisory outlines for the proof (region extraction descoped as a CV rat-hole the generation probe doesn't need); footprint-density revisit deferred until after the T3b generation probe.

### R005 — 2026-08-21 — T3a-r2 composer fixes (codex exec, gpt-5.6-terra, effort=medium)

- **Setup:** visible registration proof (outlines over master), anchor-authority placement, advisory bands. Dispatched ~15:45.
- **Verdict: PASS — and the proof did its job.** Master now clearly visible under outlines; composer mechanics accepted and committed (`893eaaa`). The readable proof confirmed the deeper truth: grammar v1's D05 data cannot support a faithful composition — major buildings unboxed or fragmentary, circulation ribbons sparse vs visible streets, anchors near-but-not-on structures. The conditional T1 acceptance's revisit clause has formally triggered → T1.1 revision lane opened.

### R006 — 2026-08-21 — T1.1 grammar D05 completeness (codex exec, gpt-5.6-terra, effort=high)

- **Setup:** whole-building capture in D05 (root-cause the fragment problem; six director-supplied major-structure seeds with ≥600 px² merged-footprint properties), D05 circulation recalibration with before/after reporting, all prior properties stay green, composer re-run so the registration proof reflects the new grammar. Dispatched ~16:05.
- **Verdict: PASS — T1.1 accepted.** Root cause found (blue dome classified water-like; merger capped at 80 px/8 px gaps). D05 footprints 25→32 with all six seeds hit inside well-fitted whole-building boxes; D05 skeleton length 666→1410 px (coverage 0.79→1.42); every prior property green. Notably rejected its own denser 3/9 calibration because inferred-link share would hit 0.438 > 0.40 — chose the honest configuration over the better-looking one unprompted. Registration proof visually confirms fit. Registered-anchor oddities (S01/S11/S15 on sparse ground) are layout-authority facts, recorded not "fixed."

### R007 — 2026-08-21 — T3b generation probe, 3 entries (codex exec, gpt-5.6-sol, effort=high)

- **Setup:** FIRST IMAGEGEN SPEND OF V2 (≤6 calls). Terrace slab + road straight + compact building per CITY-KIT-CONTRACT.md §3–§7, style-referenced from master crops via `referenced_image_paths` (headless ImageGen availability probed and confirmed). Quarantine-only; report to quarantine REPORT.md to avoid QA-REVIEW write collision with R006 (runs concurrently — disjoint file sets). Purpose: measure achievable style fidelity before committing to the 26-entry D05 kit.
- **Verdict: STYLE PASS / ALPHA WORKFLOW FAIL.** All 6 calls returned opaque RGB at tool-chosen resolution with a *drawn* transparency checkerboard — identical to the old ledger's LFX02 failure, i.e., a known property of this generator, not a prompt defect. But the subjects themselves are excellent: the compact building nails the master vocabulary (slate roof, brass trim, amber apertures, purple accent, gothic-industrial density). Director assessment: the make-or-break fidelity question is answered YES; the misses are mechanical. Sol's worker conduct was exemplary — preserved raw outputs, refused to fabricate alpha or sockets, produced honest gate tables. Watch item: projection pitch reads slightly lower than the master's 72° — side-by-side check required in the full batch.
- **Pipeline fix (F11):** generation must request a solid magenta background and a deterministic post-process keys alpha, decontaminates fringe, crops/rescales to the declared canvas, then measures real sockets from the keyed alpha. Contract §7 amended accordingly. Cross-ref: R008 (probe r2 under the chroma workflow).

### R008 — 2026-08-21 — T3b probe r2, chroma workflow (codex exec, gpt-5.6-sol, effort=high)

- **Setup:** same 3 entries under amended §7: solid-chroma generation, deterministic keying/canvas/socket post-process, gates now meaningful. ≤6 calls. Dispatched ~16:35.
- **Verdict: PASS — pipeline proven end-to-end (5 calls).** Compact building and terrace slab pass 17/17 gates with clean keying (generator's magenta drifts ±36 RGB; worker built border-connected keying with accent protection and edge decontamination — byte-identical reruns), real measured sockets, and strong style; the building's pitch now matches the master read. Road failed scale-coherence twice (generator makes wide-shallow road subjects vs the declared 80×36) — fix is measure-then-declare (F9 applied to authoring): adapt the road class's declared footprint to generator tendencies rather than fighting aspect. Slab note for full batch: more rectangular, run-friendly variants for contour-follow.
- **Sol observation:** the post-process engineering (adaptive chroma tolerance, magenta unmixing from partial edges, conservative alpha-backed footprint measurement) was unprompted quality — the packet specified *what*, Sol chose *how* well.
- **GATE:** full 26-entry D05 kit (~50–60 calls) awaits Steve's explicit go. → **Steve approved 2026-08-21 ~15:55** after confirming the generation lane runs on Codex/OpenAI-side ImageGen.

### R009 — 2026-08-21 — T3b group 1: ground + circulation, 11 entries (codex exec, gpt-5.6-sol, effort=high)

- **Setup:** first of four class-group batches (G1 ground+circulation → G2 buildings → G3 named skills → G4 props), director pixel QA between groups. Probe lessons encoded: rectangular run-friendly slabs, measure-then-declare road footprints, chroma workflow, ≤22 calls, three-consecutive-structural-failures circuit breaker. Dispatched ~16:00.
- **Verdict: PASS WITH NOTES — 10/11 entries have a passing candidate at 16/22 calls.** Clean visual passes: both retaining walls (master-vocabulary arched faces), cliff transition, both stair runs, road curve. Issues: (a) roads read near-plan with heavy curb framing — acceptable for flat surfaces, watch at composition; (b) road widths span 23.7–35.4 master px vs the grammar's ~13.5 px streets — resolvable WITHOUT regeneration by re-authoring sidecars to one width family at higher px-per-master-px density (the master-footprint mapping is authored metadata, not intrinsic); (c) slab class weakest — narrow failed gates, h090 reads road-like; borderless ground-pad re-roll deferred to a cleanup pass. Set consistency: palette/material coherent across all 11; zero matte contamination; accent survival 100%.
- **Sol batch observation:** the worker's own consistency section flagged the projection split and width incoherence before the director saw the pixels — report quality remains the strongest part of the flagship lane.

### R010 — 2026-08-21 — T3b group 2: buildings ×4 (codex exec, gpt-5.6-sol, effort=high)

- **Setup:** 2 compact + 2 standard variants, probe var01 as projection/quality benchmark, distinct massings required, ≤8 calls. Dispatched ~16:30.
- **Verdict: PASS — cleanest group yet.** 4/4 entries at ONE call each (4 of 8 budget), all 20/20 gates. The five-building set (incl. probe var01) reads as one neighborhood with genuinely distinct massings (gabled workshop, corner-turret L, attached pair, tower+annex civic, U-shaped guild hall). Worker even upgraded the keying predicate mid-run to protect saturated interior purple highlights. Watch item (worker self-flagged): detail-kit repetition — every building uses the same purple-tube/finial/amber-window vocabulary; composer should interleave variants, and G3 prompts explicitly vary accent placement.
- **Trend note:** call efficiency improving per group (probe 6→5, G1 16/22, G2 4/8) as prompt patterns and the workflow mature — the repeatability curve Steve wanted to measure, showing up in the spend data.

### R011 — 2026-08-21 — T3b group 3: named skill buildings ×5 (codex exec, gpt-5.6-sol, effort=high)

- **Setup:** S01 foundry / S10 tiered works / S11 warehouse+crane / S15 observatory / S18 wheelworks — identity-dominant silhouettes per Steve's capital-identifiability parameter, anchor-local style crops, capital-scale legibility strip required in report, ≤10 calls. Dispatched ~17:05.
- **Verdict: PASS WITH ONE DEFECT — and the identity parameter validates.** 5/5 at one call each, 20/20 gates. Five genuinely distinct identity-dominant massings; the capital-scale strip (26–27 px declared widths) shows chimney/ziggurat/dome/wheel all surviving as readable silhouettes — Steve's capital-identifiability requirement is achievable. Defect the gates couldn't see: crane/gantry elements on S01/S10/S11 rendered saturated magenta (generator echoing the chroma background into accents) → deterministic recolor fix dispatched (R013), no regeneration. Anti-repetition prompting worked — no G2 purple-tube-layout cloning.
- **Flow note → F12 candidate:** chroma-background generation can leak the key hue into subject accents; add "metalwork never magenta/pink" to all chroma-workflow prompts and an interior-magenta-cluster check to the gate set.

### R013 — 2026-08-21 — G3 crane recolor fix (codex exec, gpt-5.6-terra, effort=medium)

- **Setup:** deterministic luminance-preserving hue shift of interior magenta clusters to dark iron on S01/S10/S11; provenance copies kept; gates rerun; sheets rebuilt. No ImageGen. Runs concurrently with R012 (disjoint quarantine dirs).
- **Verdict: COMPLETED — then superseded within the hour.** Steve reviewed G3 on the console and rejected the set for a reason neither gates nor director QA had covered: **no technology iconography** (his Go concept building wears a gopher face; these are generic industrial). Owner parameter amended into the contract (§9.2: in-world tech motifs, never text/logos), r01 set superseded, R014 re-roll dispatched with per-tech motif specs and Steve's own concept art as the S01 reference. The recolor's F12 lesson (magenta-bleed prompt rule) carries into all future prompts.
- **Flow finding → F13:** owner design review on the console caught a requirement-class gap (thematic identity) that no property or director pass had encoded — the visibility dashboard is itself a QA layer. Owner-visible evidence early = cheaper corrections; this one cost one group re-roll (~5-10 calls) instead of a composed-district rejection.

### R014 — 2026-08-21 — G3-r02: named skills with tech iconography (codex exec, gpt-5.6-sol, effort=high)

- **Setup:** same approved massings + gopher/red-bricks/whale/keyhole/tools motifs as physical architecture; Steve's concept frame as S01 reference; no-magenta rule in every prompt; ≤10 calls. Dispatched ~17:35.
- **Verdict: 4/5 ACCEPTED (one with a tone fix), 1 re-archetyped.** 5/5 at one call each, 21/21 gates. S01's gopher is masonry-built and exactly the owner's concept register; S11's whale counterweight is subtle-good; S18's wrench crest passes; S10's motif is unmistakable but the oxide red is a chroma siren vs the near-black city → deterministic desaturation fix (R016, asset-level because canonical assets ship world-wide). S15's keyhole passed gates but stayed semantically thin → Archive re-archetype dispatched (R015) per the pre-agreed direction. Worker report quality note: it flagged both the S10 chroma dominance and the S01 tone difference before director review — third consecutive group where Sol's self-reads anticipated the director's findings.

### R015 — 2026-08-21 — S15 OpenAPI Archive re-archetype (codex exec, gpt-5.6-sol, effort=high)

- **Setup:** vaulted scriptorium — barrel vault + lantern tower silhouette, scroll racks through windows, open-tome statuary, no-text schematic murals; ≤4 calls. Dispatched ~18:20.
- **Verdict: PASS — best building in the set.** Candidate B: glowing scroll racks through every window, hooded scribe statues with open tomes, flank murals that are literal line-and-node schema diagrams (no text), distinct barrel-vault + lantern silhouette. Worker self-rejected candidate A on semantic grounds despite 21/21 mechanical passes — the gates-vs-meaning distinction is now internalized in the worker layer. S15 manifest label change (Observatory → Archive) queued for the D05 swap.

## KIT COMPLETE — 2026-08-21 ~18:40

**25 entries banked at ~50 ImageGen calls** across probe (2), G1 ground+circulation (10), G2 buildings (4), G3-r02 named+archive (5), G4 props (4). Two composer-demand fill-ins deferred per F9 (road-curve-right, narrow slab). Every part: chroma-keyed alpha, measured sockets, 20-22 mechanical gates, director pixel review, quarantined pending the composed-district review. **R017 (T3c composition) dispatched** — real sprites replace placeholders, road width-family re-authoring, contact shading + cast shadows, moody-but-legible grade, side-by-side vs the master's D05 crop.

## Skill-roster audit — 2026-08-21 ~18:55 (owner + director)

Steve's test: a skill building is valid only for a recruiter-searchable technical skill — never soft skills or project-internal concepts. Audit of the 19 registered nodes: 16 clean (Go, TS, React, TanStack, C#, Python, PostgreSQL, Redis, AWS, Databricks, Docker, VMware, MacStadium, gRPC, OpenAPI, MCP). Three failed/flagged:
- **S18 "tool-generation-wheelworks"** — project artifact (Steve's mcpgen tool), not a skill. Anchor reassigned to **Node.js** (evidenced: the repo's entire authored build toolchain; absent from roster; top recruiter term). R018 generates `nodejs-millworks` with hexagon iconography; the wheelworks asset is shelved for mcpgen's future project city. R017's in-flight composition uses wheelworks as a stand-in at the anchor; placement swaps after R018 passes.
- **S17 "capability-citadel"** — fails the test; reclassified as civic landmark, removed from the skill index (manifest change queued with the S15 rename for the D05-swap lane).
- **S19 "ai-palace"** — skill valid (agentic/AI systems is recruiter-searchable), name and palace-flavor need rework; deferred (not in D05).
- **Flow finding → F14:** registered data inherited from prior agent eras carries unaudited semantics — names Sol treated as authority in the contract were never owner-validated. Roster-class data needs an explicit owner audit gate before assets bake it in; caught here at a 4-call cost because the canonical-asset rule made naming permanent.

### R018 — 2026-08-21 — S18 Node.js Millworks (codex exec, gpt-5.6-sol, effort=high)

- **Setup:** millworks archetype (fits the anchor's wheel neighborhood), hexagon iconography + muted Node green, distinct silhouette vs S01/other wheels; ≤4 calls. Dispatched ~18:55, concurrent with R017 (disjoint dirs; report stays in quarantine).
- **Verdict: PASS — one call.** Hex rose window, six-sided hub, hex paving, green glazing/banners: unmistakable to a Node developer, pure millwork to anyone else. Distinct 27 px silhouette (broad gable + wheel). Worker self-detected 5,963 magenta metalwork pixels via the F12 gate and fixed them deterministically with provenance before reporting — the full defect-detect-fix loop now runs inside a single worker without director intervention. Roster corrected: S18 = Node.js; wheelworks shelved for the mcpgen project city.

### R017 — 2026-08-21 — T3c: compose D05 from the real kit (codex exec, gpt-5.6-sol, effort=high)

- **Setup:** the milestone packet. Deterministic composition, no ImageGen. Honest-report requirement on composition gaps. Dispatched ~18:40.
- **Verdict: MACHINERY PASS / VISUAL FAIL — best failure report of the project.** 190 placements, zero property violations, byte-identical rebuilds, road family unified at 14 px — and the worker correctly declared the output "not a master-conforming visual candidate" with a ranked 5-item demand list instead of dressing it up. Root causes: (1) DIRECTOR ERROR (→F15): building scale mapping inherited grammar roof-core footprints (~27 px) when the registered layout's displayWidth (130-200 px) was the authority — buildings rendered ~7× too small; (2) DIRECTOR ERROR: proof composited over void though the city layer never owns terrain — unjudgeable by construction; (3) 75% of circulation length off the two authored axes (diagonal variants needed); (4) short-run overhangs (end-cap policy); (5) wall banding (re-judge after scale fix). R019 fixes 1/2/4 with zero generation calls.

### R019 — 2026-08-21 — T3c-r2: scale authority + terrain underlay + end caps (codex exec, gpt-5.6-sol, effort=high)

- **Setup:** re-author all sidecar scale mappings from registered display widths (metadata only), composite proofs over the master terrain underlay, end-cap clipping, refreshed demand list ranked by visual impact at correct scale. Dispatched ~19:15.
- **Verdict: PASS — first genuinely encouraging proof.** Full scale re-authoring recorded (named 175-200; class widths from layout stats p25/p50/p75 = 170/175/180; all 32 placements within 2.78% of target); 77 short runs end-clipped; kit-over-master proof shows the composed city reading as the same family — correct scale, believable grounding, coherent style. Remaining ranked gaps: (1) 40% building-pair overlap (density/occupancy), (2) diagonals, (3) generic-large entries, (4) wall banding (downgraded). Plus one new artifact: a pink ghost region center-left. Director assessment: the pipeline's visual thesis now looks likely to hold; the double-city interim baseline (master's baked buildings under ours) is the main obstruction to a clean judgment → clean-terrain underlay next.

### R020 — 2026-08-21 — T3c-r3: clean-terrain underlay + occupancy + ghost (codex exec, gpt-5.6-terra, effort=high)

- **Setup:** derive clean D05 terrain from registered native land assets (read-only; report if world-registration is ambiguous rather than guessing), 15% pairwise overlap cap with priority placement, identify the pink ghost. Dispatched ~19:50, concurrent with R021.
- **Verdict:** pending.

- **R020 verdict: MIXED — good derivation discipline, three new defects exposed.** Underlay derivation was registration-correct but used the WRONG asset family (world relief instead of the NinjaOne environment plates — director packet error); the proof revealed a real composer alpha leak (all sprites render translucent; r2's "pink ghost" was S10 at low alpha all along); and the 15% occupancy cap proved style-false, pruning 24/32 including two named buildings whose registered anchors legitimately overlap (the master's density IS heavy z-sorted overlap — director spec error, F9 pattern: cap set without measuring the master). Ghost root cause honestly identified by the worker. All three fixes in R022 with the occupancy cap now derived from master-measured overlap stats.

### R022 — 2026-08-21 — T3c-r4: native underlay stack + opacity bug + style-true occupancy (codex exec, gpt-5.6-terra, effort=high)

- **Setup:** environment-plate underlay (geology/surface/conifers — the runtime's real under-city stack), alpha-leak root-cause with a draw-path opacity unit check, occupancy cap from master p90 pair-overlap with named-always-place. Dispatched ~20:20.
- **Verdict: 2/3 FIXED, ghosting persisted → director hands-on debug (standing rule honored).** Underlay now the correct registered environment stack (geology r8 + surface detail + 59 conifer instances, exact 4x crop registration); occupancy now master-measured (p90 directional cap 64.5%; 19/32 survive incl. all 5 named — vs 8 under the arbitrary 15% cap). But buildings still ghosted, and the worker's opacity check ("≥1 opaque pixel survives") passed anyway. **Director in-shell diagnosis (10 min): extracted the S10 region — the sprite draws as alternating horizontal scanlines.** Root cause: hand-rolled resampler iterates source pixels and pushes to rounded destination rows, skipping rows at non-integer scales. Two findings: **F16** (solidity, not existence: region-level opaque-fraction properties, ≥0.98 within source coverage) and the meta-lesson — after two same-symptom worker failures, ten minutes of director pixel-sampling beat a third blind revision.

### R024 — 2026-08-21 — T3c-r4b: destination-space resampling fix (codex exec, gpt-5.6-terra, effort=medium)

- **Setup:** surgical — replace push-resampler with destination-space sampling (or sharp resize+composite), F16 solidity property ≥0.98, regenerate proofs. Dispatched ~21:05.
- **Verdict: HONEST NULL RESULT — my resampler hypothesis was wrong.** The worker found the resampler already destination-space (sharp lanczos3), refused to fabricate the defect I described, added the F16 solidity property (all 19 buildings ≥0.9986), and the stripes persisted. Director follow-up in-shell narrowed it decisively: underlay panel clean, sprite+resize clean in isolation → a per-placement composer pass (mask clip / contact / shadow / fringe-cleanup) stripes the kit layer. Prime suspect: a dithered/progressive mask in the clip chain. → R025 binary-searches the pass with the evidence attached. Worker refusing to "find" a bug that wasn't there is exactly the honesty the contract buys.

### R025 — 2026-08-21 — T3c-r4c: pass isolation binary search (codex exec, gpt-5.6-terra, effort=high)

- **Setup:** toggle-rebuild each alpha-touching pass over the S10 region, identify the striper, fix, add per-row oscillation property. Dispatched ~21:30.
- **Verdict: PASS — bug found and killed.** The striper was the district-mask clip: Sharp expanded the resized 1-channel mask to 3 channels and the composer indexed it 1-byte-per-pixel, consuming `255,0,0` as three pixels → one visible row in three. (My dithered-mask suspicion was wrong; the binary-search protocol found the truth anyway — that's why it's a search, not a guess.) Fixed with forced greyscale + per-pixel assertion + direct alpha multiply; F16 oscillation = 0 for all 19 placements. Director-verified by independent S10 re-extraction: fully solid. **The r4c side-by-side is the pipeline's proof-of-thesis: a genuine coherent city at master scale and density on native terrain.** Remaining demand (ranked): dusk-mood grade pass, native-foliage in-fill of the 13 unbuilt slots, diagonal integration, compact/standard variant repetition relief.

### R021 — 2026-08-21 — T3b-G5: diagonal circulation family ×4 (codex exec, gpt-5.6-sol, effort=high)

- **Setup:** road h045/h135 + diagonal stair pair, width family fixed at 14 master px matching the r2 world scale, separately authored (no mirroring), ≤8 calls. Report quarantine-only (R020 owns QA-REVIEW).
- **Verdict: GATE MATH WRONG, PARTS LIKELY RIGHT.** 8 calls; only 1/4 mechanically green — but the three "failures" all measured ~56 px module length vs the ~40 px axis bound, which is exactly 40×√2: the director's length spec ignored that diagonals spanning the same grid distance are √2 longer by construction. R023 corrects the gate and re-evaluates (no regeneration). Worker also correctly surfaced and ruled on a heading-convention conflict between packet and contract with documented per-sidecar deviations — model behavior exemplary, spec behavior (mine) the recurring bottleneck. → reinforces F9/F15 family: geometry claims in specs need the arithmetic done first.

### R023 — 2026-08-21 — G5 gate correction, √2 diagonal bound (codex exec, gpt-5.6-terra, effort=medium)

- **Setup:** corrected module-length gate for diagonal headings, full re-evaluation, medallion-mute variant if road-h045 A is sole passer. Dispatched ~20:45.
- **Verdict: PASS — 2/4 diagonals green under correct math.** Road h045 A (56.64 vs 56.57+ε — the √2 prediction to two decimals) and stair NW B pass 23/23; h135 (8% long) and stair NE (short) genuinely fail. Notably the worker retracted its own earlier NE-B green as a wrong-gate artifact — self-correcting against its own prior claim. Medallion-mute comparison variant produced with provenance. Re-roll decision for the two failures deferred to the r5 heading-demand histogram (F9: measure before spending).

### R016 — 2026-08-21 — S10 chroma tone-down (codex exec, gpt-5.6-terra, effort=medium)

- **Setup:** deterministic ~35% desaturation + slight darkening of the tier faces, kiln glow and windows preserved, provenance kept, gates rerun. Concurrent with R015 (disjoint dirs). Dispatched ~18:20.
- **Verdict: PASS.** Tiers now read as fired oxide in shadow — recognizably red, no longer a beacon; glow seams and windows untouched; gates green; before/after crops and provenance retained. S10 canonical asset finalized.

### R012 — 2026-08-21 — T3b group 4: props ×4 (codex exec, gpt-5.6-sol, effort=high)

- **Setup:** lamp+bollard, crates+barrels, bench+signpost, utility beacon; prop-scale measure-then-declare; "metalwork never magenta" prompt rule from the G3 lesson; ≤8 calls. Final generation group.
- **Verdict: PASS — 4/4 at 5 calls, and F12 closed its loop.** The worker promoted the magenta-bleed lesson into an actual gate (22/22 now), which caught real pink contamination on bench candidate A; candidate B fixed it under the named-defect protocol. Prop-vs-building scale ratios verified sane (lamp 2.5 vs compact 13.3 master px). Kit status: 20 entries banked + 5 in flight (G3-r02). Two potential composer-demand fill-ins deferred per F9: road-curve-right, narrow slab.

### R026 — 2026-08-21 — T3c-r5: dusk grade + foliage + diagonals (codex exec, gpt-5.6-terra, effort=high)

- **Verdict: PASS — presentable to the owner.** Director picks: **medium grade** (gain .72 / sat .92 / floor #101418 / warm-mask emission boost — the composed side now lives in the master's tonal register) and **muted medallions** (near-invisible in-context; muted is safer on exposed runs). Foliage: 79 deterministic native placements pass the coverage floor, but the floor itself was computed from a high-specificity palette sample that likely under-measures the master's true forest density (F9/F15 family — proxy under-measurement); composed forest reads sparser than master. Diagonals: h045 installed; NW stair honestly withheld (no in-tolerance run); heading histogram says h135/NE would service only +24.5% of off-axis length — deferred, circulation is largely occluded at true density anyway. Honest flag carried: full-suite gate unverified (worker hung on the known slow tests) — must pass before the runtime-swap lane ships.
- **Residual demands (non-blocking, incremental lanes):** forest density pass with corrected master sampling; compact/standard variant breadth; h135/NE if a future district demands them.
- **Owed gate cleared (director-run, ~21:20):** full suite `209 pass / 0 fail / 2 skipped` in 450 s. The swap lane's only hard prerequisite is green. All v2 work to date coexists with the legacy suite untouched.

## CONCEPT APPROVED — 2026-08-22 (Steve on candidate A: "this is so much better")

Bright-register candidate A is the accepted D05 visual concept. Remaining path to runtime: R033 anchor verification (F19 pass, in flight) → mountable-plate prep → runtime mount lane (features/ L4 scope, D05-first per the earlier accepted §9.3 parameter) → Steve sees it live at localhost:3000 before any push.

### R033 — 2026-08-22 — anchor verification on candidate A (codex exec, gpt-5.6-sol, effort=high)

- **Setup:** locate the 5 named buildings, emit registration JSON (anchors in candidate + master space), derive the runtime mask, marked overlay proof. Zero generation.
- **Verdict:** pending.

### R032 — 2026-08-22 — T3d-v3 bright-register integration (codex exec, gpt-5.6-sol, effort=high)

- **Verdict: CONCEPT-LOCK CANDIDATE — presented to Steve.** Candidate A: warm afternoon register (measured 1.64× master luma, 2.1× saturation — mechanical description, not authority), blue-dome accent vocabulary returned, varied roof rhythm, forest interpenetration retained, all five named identities verified in-frame with animation cues visible (chimney, kiln glow, crane, lantern tower, great wheel). F17 worked hard this run: three discards (identity hybridization, out-of-frame whale, erased archive on a local-edit retry — the second local-edit invariance failure, consistent with F18's no-surgical-edits envelope). Director read: this answers all three critiques from the owner's last review; it is concept/reference evidence, not a registered plate (drift expected, anchors post-hoc per F19).

### R031 — 2026-08-22 — T3d-v2 dusk integration with full identity brief (codex exec, gpt-5.6-sol, effort=high)

- **Verdict: IDENTITY BRIEF PROVEN (register obsoleted mid-flight by Steve's bright redirect).** Candidate C1: all five named identities survive whole-scene integration with dense interpenetration and continuous rock-cut earthworks. F17 catch of the week: the model painted a DUPLICATE gopher foundry (candidate C) — discarded for identity non-uniqueness, retry corrected. Transferable: identity-reference sheet + verbatim brief = named survival; anchors post-hoc per F19. Dusk register shelved; R032 (bright) is the live target.

## Owner review pivot — 2026-08-22 (the r5 rejection and the method correction)

Steve rejected the r5 composition on two grounds the director's QA under-weighted: (1) cohesion — asset-stitching read, missing embedded forest/interstitial fabric/earthworks; (2) identity — generic buildings, wrong scale (director's third scale error: class widths derived from district-fabric node stats → compacts 3× oversized; scale-audit measured truth: compact 54 / standard 93 / large 158), off perspectives, "cut from a different art page." Steve's design corrections, now contract law (§6.5): **in-context generation over the real terrain backdrop with diff-extraction against the known background** (the old pipeline's own method, rediscovered); a **city-owned transition layer** (the generation halo = earthworks/foliage blending assets into the mountain); fabric as neighborhood cluster patches; **discard-don't-repair** (F17). Probes: R027 whole-plate integration (in flight), R028 scale/perspective/grounding audit (PASS — measured fingerprint + vocabulary), R029 in-context probe (S10 re-grounded + compact cluster, in flight). Strategy question parked with Steve: capital end-state (master-keeps-capital + pipeline-proves-on-project-cities vs full capital rebuild).

### R027 — 2026-08-22 — T3d whole-plate integration probe (codex exec, gpt-5.6-sol, effort=high)

- **Verdict: SPLIT, exactly as the worker framed it.** Cohesion hypothesis SUPPORTED — candidate C reads as one painted city in the master's family (continuous fabric, terracing, embedded forest edges, S10/S18/S11 recognizable). Registered-plate hypothesis NOT supported — geometry drifts from the scaffold and a retry repeats the drift; prompt-only position locks fail. 4/6 calls, honest report, raw outputs + full provenance retained.
- **Director synthesis (pending R029):** invert the registration assumption — scaffold controls content, integration paints cohesion, then **re-extract exact registration from the integrated plate with the T1 grammar extractor** (its second consumer). Drift becomes acceptable if identity survives; positions need to be *known*, not *predetermined*. Candidate recipe: compose → integrate → re-extract → mount.

### R028 — 2026-08-22 — master scale/perspective/grounding audit (codex exec, gpt-5.6-terra, effort=high)

- **Verdict: PASS.** 25 buildings measured manually with annotated overlays; class widths compact 54 / standard 93 / large 158 (vs the mis-derived 170/175/180 — F9/F15 third occurrence: the node stats measured districts, the component stats measured fragments; only visible-building measurement measures buildings). Perspective fingerprint + grounding vocabulary (terraced pad / rock-carved / cantilever / tree-hugged) delivered as reference crops.

### R029 — 2026-08-22 — T3e in-context probe (codex exec, gpt-5.6-sol, effort=high)

- **Setup:** S10 re-grounded at anchor + compact cluster patch, in-context over pristine native terrain, diff-extraction, halo kept as transition content, gates as discard triggers, ≤10 calls. Dispatched with the audit's measured numbers.
- **Verdict: DECISIVE NEGATIVE — clean kill at 4/10 calls.** 0/4 within ROI; the generator repaints the entire frame every time (500× over tolerance), so pixel-preserving edits and therefore mechanical diff-extraction are impossible on this tool → **F18**. Worker executed discard-don't-repair perfectly (one-line discard facts, no salvage). Visual premise confirmed in passing (discarded candidates looked properly embedded) — right idea, wrong tool capability.
- **Method comparison resolved:** the generator wants to paint whole scenes → pipeline embraces it: **compose scaffold → whole-plate integration → re-extract registration → mount.** Per-asset surgical edits are off the menu; single-asset changes go scaffold-edit → re-integrate → re-extract. R030 validates the re-extraction leg against integration candidate C (zero generation).

### R030 — 2026-08-22 — re-extraction validation on candidate C (codex exec, gpt-5.6-terra, effort=high)

- **Setup:** run the T1 extraction machinery against the integrated plate; recover footprints/circulation/named positions; report drift vectors vs scaffold intent and runtime-usability. Dispatched immediately (informs Steve's method decision).
- **Verdict: HONEST NEGATIVE that right-sized the requirement.** The r3 classifier (master-palette-calibrated) recovers only fragments from C — automated full re-extraction does not transfer. But the probe's own visual pass located all three named buildings with drift vectors, exposing that full geometric re-extraction was over-spec: **the runtime consumes plate + district masks (scaffold-derived) + named anchors only** — footprint censuses and circulation topology are composer-side artifacts. → **F19**. Recipe final form: **compose → integrate → verify/adjust named anchors (minutes-long visual pass) → mount.** All legs now evidence-backed; both decisions (method + capital strategy) fully briefed for Steve.

### R034 — 2026-08-24 — T8a2 fresh S10/S11/S18 skill sprites (headless Codex worker; model slug/effort not exposed)

- **Setup:** 11:52–12:08 CDT; 3 scheduled ImageGen calls + 1 reserve; same registered crops, current selected candidates, prompt contract, and global extended chroma key; exact neutral-plume guard on every call; quarantine only.
- **Verdict: BLOCKED — 1 newly valid selection, 2 assets still blocked.** S10-B passes all mechanical gates with no plume. S11-C reproduces the prior one-pixel Section 7 failure. S18-D/E both violate the simple six-sided wheel identity before mechanical processing, so the 4-call budget closes with S18 still on prior invalid C evidence. Aggregate remains 3/5.
- **Model-attributable observation:** the generator followed the no-plume constraint for all four calls, but repeated a one-pixel chroma-edge defect on S11 and failed the exact polygon-side constraint twice on S18 even when the reserve prompt explicitly excluded 8/10/12/many-sided rims. Exact-count geometry remains unreliable enough that F17 identity discard must precede downstream keying.
- **Evidence:** `.codex-tmp/quarantine/city-v2/T8a-skill-sprites/{REPORT.md,proof-manifest.json,comparisons/skill-sprites-contact-sheet-r1.png}`. Deterministic process rerun changed 0/30 non-report/non-manifest artifacts; expected aggregate exit remains 1 on S11/S18.

### R053 — 2026-08-24 — T8a7 S18 solid-wheel regeneration (codex exec, gpt-5.6-sol, effort=high)

- **Setup:** 2-call bounded budget (1 scheduled + 1 reserve); `references/s18-reference-4x.png` sole authority; one new enumerated delta — visually solid wheel (closed backing face or flush stone flank), explicit no-see-through-gaps prompt negative, new raw-stage pocket check (gate 1b) before processing; v2 radius-2 key only (key extension closed by T8a6); accepted-sprite hashes a hard invariant. ~228k worker tokens.
- **Verdict: BLOCKED — the packet's target defect is solved; a different defect class killed the candidate.** K passed gate 1 and gate 1b (0 enclosed pockets — the solid-wheel brief genuinely eliminates trapped background) and every mechanical gate, then failed the final keyed-wheel 2x eye gate: a thin magenta fringe along the wheel's outer rim, with §7/F12 again reporting zero (third false negative → F21). Reserve L castle-drifted at gate 1, unprocessed. Protected hashes unchanged. Lane suspended by director; escalated to Steve (hold / bounded despill needing an F17 ruling / re-brief without the wheel).
- **Model-attributable observation:** sol executed the new solidity constraint exactly on the scheduled call (continuous closed timber face, zero pockets) — enumerated *positive* geometry constraints with an explicit negative land reliably. But the reserve repeated the castle/monumental-showpiece drift already seen in the T8a4 and T8a5 reserves under near-identical prompts: on this asset, second samples regress toward grandeur defaults. And the asset-level pattern is now measured: S18's curved openwork silhouette maximizes keyed-boundary length, making it uniquely fringe-prone — 10 failed candidates (C–L) vs ≤2 calls for each of the other four sprites through the identical workflow.
- **Evidence:** `.codex-tmp/qa/T8a7-s18/{s18-k-raw-wheel-closeup-2x,s18-wheel-closeup-2x,skill-sprites-contact-sheet-r1}.png`; quarantine `REPORT.md`/`proof-manifest.json` (status `failed`); QA-REVIEW.md T8a7 worker entry + director verdict.

### R054 — 2026-08-24 — T8a8 S18 despill (codex exec, gpt-5.6-terra, effort=high)

- **Setup:** owner-authorized (F17 carve-out) color-only despill on candidate K; hard invariants: byte-identical alpha, accepted sprites untouched; ≤2 recorded parameter sets; zero ImageGen. ~172k worker tokens.
- **Verdict: PASS, director-confirmed by eye.** Set 1 (radius 2, blend .82) removed magenta but left a pale neutral halo — honestly failed against F21. Set 2 (radius 3, saturation ≥0.24, blend .92) recolored 3010 boundary pixels with clean neighborhoods; fringe gone at 2x, no halo, alpha byte-identical, all mechanical gates green, four protected hashes unchanged. Selected S18 `3a79d351…fef843`, quarantine-only; owner sign-off gates the mount.
- **Model-attributable observation:** terra executed the two-set tuning protocol exactly as bounded — reported the set-1 halo failure rather than silently widening, and the blend/radius direction of its second set was the correct correction. Deterministic image-processing lanes with numeric invariants remain a terra sweet spot (fifth clean terra mechanical lane).
- **Workflow lesson banked:** generation-time solidity (T8a7 gate 1b) + boundary despill is the complete recipe for chroma artifacts on openwork silhouettes; key extension (T8a6's dead end) was never the right layer for this defect.
- **Evidence:** `.codex-tmp/qa/T8a8-s18/{s18-wheel-closeup-2x-despilled,skill-sprites-contact-sheet-r1}.png`; QA-REVIEW T8a8 entry + director verdict; quarantine provenance records `despill-v1` params.

### R055 — 2026-09-01 — L2 cell c4-2 replacement bake through `cell.mjs` (codex exec, gpt-5.6-sol, effort=high)

- **Setup:** the solidified cell pipeline dispatched ONE worker with the pipeline-built packet (concept-composited neighbour context c4-3 + c3-3, two binding survey pegs measured from the current c4-3: stream centre at 19.5% of the canvas, no sea on the south edge); worker delivers one 1254 px generation + a same-size water mask; the pipeline derives, bridges (48..150 px), gates and stitches. `12m46s` wall, `140,967` worker tokens, 2 ImageGen calls.
- **Verdict: REJECTED by three gates — world unchanged.** Water continuity: crossing at gen-x `8644` vs the neighbour's `8435`, a `209 px` miss (bridge reach 150). Water fringe `8.45%` (limit `<1%`). Palette `dBG 0.301` at the c4-3 seam (limit `0.18`). Key-light `0.0021` PASS. Director reproduction of the fringe classifier at rings 6/48: accepted c4-3 `0.22%/0.18%`, c3-3 `0.00%/0.01%`, candidate `7.78%/6.27%` — the worker built its mask as a 16 px polyline, leaving a ~40 px strip of painted stream water beside the cut.
- **Model-attributable observation:** sol's process discipline was exemplary and its geometry was not. It correctly refused the stale quarantined candidate as evidence-only, stopped at the packet's two-candidate limit, and reported its own peg miss as a BLOCKER in the report (`south_stream_alignment.pass: false`, `delta_final_px 179.56`) instead of claiming compliance — a worker self-report that was honest and still wrong on magnitude (the gate measured 209). But two consecutive whole-canvas generations missed an explicit coordinate peg by ~350 then ~210 px at 2560 (14% and 8% of the frame) even with coordinates in its own prompt: this generator's positional precision on a stated peg is roughly a tenth of the frame. And it authored the mask by hand-placed vector polylines rather than from painted pixels — a shortcut the packet's "entire painted extent" rule did not prevent (procedures over principles: the rule needs a gate, not wording).
- **Workflow lesson banked:** anything that must land on a coordinate is the pipeline's job (the bridge exists for this; its reach is the lever, and it is an owner call because the reroute leaves an orphaned painted channel — mock rendered for the decision). A mask-misfit gate at a wider ring is calibrated and ready (accepted `0.18%/0.01%` vs candidate `6.27%` at ring 48). Bakes take ~13 min, not the 35 the handoff estimated. The worker also runs `git status` on start and swallows the worktree's 400 uncommitted stream-r3 lines into context each bake.
- **Evidence:** `.codex-tmp/session3/bake-c4-2.log` (gate ruling), `.codex-tmp/authoring/cells/c4-2/{c4-2.log,c4-2-report.json}`, `.codex-tmp/session3/review/final/sheet-5-c42-candidate.png` (raw join, mock bridge), `.codex-tmp/session3/{peg-evidence,misfit}.mjs`.
- **Ledger note:** the ten L2 bakes of the 2026-09-01 sessions (three cells, six rejections) were recorded in `STATE.md` and the branch's commit messages, not here; the c3-3 acceptance this session was a `--redo` (no worker).

### R056 — 2026-09-01 — Probe: built-in `image_gen` EDIT mode as an outpainter, take 1 (codex exec, gpt-5.6-sol, effort=medium)

- **Setup:** director-built edit target, 2560x2560: the neighbours' CONCEPT paint (c4-3 kept + bleed, c3-3 at the corner) in the bottom 512 px, flat neutral grey elsewhere; prompt = the c4-2 brief rewritten as a continuation with no coordinates, opening "Extend this hand-painted terrain map upward". Worker procedure: `view_image` the target, ONE `image_gen` call in EDIT mode, deliver the raw output and a tool-truth report. `37,338` tokens, ~2 min wall. Motivation: every generate-mode bake reproduces the neighbour pixels it was shown at correlation ~0 (measured on all three cells), which is why seams read and pegs miss.
- **Verdict: mechanism confirmed, framing lost.** Tool truth recorded by the worker: edit mode exposes only `prompt`, `referenced_image_paths`, `num_last_images_to_include` — no mask, no input fidelity, no output size, no region. Output came back `1024x1536` portrait from a square input. The given band was RE-RENDERED following its layout, not preserved: luma correlation with the real band `0.357` under the stretch mapping (crops `0.06–0.16`), against an unrelated-band baseline `0.143` and `1.000` for preservation. The stream continued through the boundary row with no jog (eyes), ~22 px from the quarry's entry.
- **Model-attributable observation:** the worker executed the procedure exactly and reported the interface honestly. The aspect change is a prompt effect: "extend upward" was read as "make it taller".
- **Evidence:** `.codex-tmp/session3/probe/{result-c4-2-builtin-edit-1.png,builtin-edit-report.json,builtin-edit-1.log,builtin1-side-by-side.png,builtin1-join-stream-1to1.png}`; measurement `probe-measure.mjs`.

### R057 — 2026-09-01 — Probe take 2: same edit mode, framing pinned in the prompt (codex exec, gpt-5.6-sol, effort=medium)

- **Setup:** identical target and procedure; the prompt now opens by pinning a SQUARE output with the same framing and extent, the painted band staying in place at the same scale, no crop or zoom. `43,134` tokens, ~2 min.
- **Verdict: this is the outpainter, and it needs no API key.** Output `1254x1254` — exactly the pipeline's `MIN_SRC`. The band follows the quarry's real paint at `r 0.739` (MAD `17.9`; per 64-row sub-band `0.69–0.76`; resolution-ceiling control `1.000`, because the quarry band is itself a 1254 upscale, so the shortfall is model fidelity, not resampling; unrelated baseline `0.143`; generate-mode bakes `~0.00`). Join row at the mask boundary: mean step `18.8` vs interior lines `14.7–20.0` — no join. **Simulated stitch along the REAL c4-2|c4-3 ownership curve** (probe above, quarry below, `probe-stitch-sim.mjs`): mean step `20.2` / median `15.9` / p90 `44.5`, inside the no-seam interior range (`15.6–22.7`), against the current world's `26.2 / 22.6 / 51.3`. The stream reaches the shared line within ~10–20 px of the quarry's crossing at `498` (tick-marked crop, eyes) — inside the 48 px continuity gate, no bridge needed. Seam not findable by eye at 1:1 in the crop. Style, neutral lighting and composition (tarn, fall, gully, ledge, east sea-cliff, dry south edge) read right by eye; the real gates would judge a real bake.
- **Workflow lesson banked:** F18 refined — the built-in edit mode is composition-following (`r 0.74`), not pixel-preserving; F11 refined — the tool's framing CAN be pinned by an explicit prompt preamble (n=1 each way). The peg problem dissolves when the model is handed the neighbour's PAINT as the edit target instead of a prose coordinate; the seam problem drops to interior-level by measurement. Proposed pipeline change (owner approval, `cell.mjs` is solidified): build `context/edit-target.png` (neighbour concept paint in the overlap band, neutral fill elsewhere, 2560 square) and mandate ONE edit-mode call with the framing preamble and the brief; keep the wide-ring mask gate proposal, since workers still author the water mask. Caveats: one sample per probe; replication is the first real bake's gates.
- **Evidence:** `.codex-tmp/session3/probe/{result-c4-2-builtin-edit-2.png,builtin-edit-report-2.json,builtin-edit-2.log,builtin2-stitch-stream-1to1.png,builtin2-stitch-stream-CURRENT-1to1.png,builtin2-stitch-band-half.png,builtin2-join-band-half.png}`; scripts `probe-target.mjs`, `probe-measure.mjs`, `probe-stitch-sim.mjs`; packets `packet-builtin-edit{,-2}.md`; prompts `prompt-c4-2-outpaint{,-square}.md`.

### R058 — 2026-09-01 — Seed r2 round 1: three register candidates (codex exec, gpt-5.6-sol, effort=high)

- **Setup:** owner-approved register change (epic, fantastical, light-toned; classic 2000s painted fantasy card-art feel translated into this world's projection and rules). Three whole-canvas GENERATE-mode candidates at three intensities (a grounded epic, b fantastical, c overtly magical), r1 seed as the single reference for view/scale/density, register by words. `66,016` tokens, 3 ImageGen calls, ~7 min.
- **Verdict: register hit, lighting missed, ground state too uniform.** Owner: "much closer to the direction I want"; his crop of c's gorge (floating fragments, crystals, glowing fall) became the target for the interesting areas. The key-light GATE passed all three (`0.0007–0.0021`) while the owner's eye saw a consistent lit side; the director's hue-isolated strong-edge moment inside rock only measured `0.24 / 0.18 / 0.27 @ ~50°` against the accepted quarry's `0.074` — a stable direction across three independent generations, i.e. an implied key the gate cannot see because grass gradients dilute it. Owner also: "less happy meadows everywhere", scenery must vary.
- **Model-attributable observation:** the packet itself asked for "pale warm basalt with cool shadow sides" — a director-authored lit-side instruction. sol delivered it faithfully. Scale, projection and density held from the single reference; the worker wrapped every prompt in the imagegen skill's scaffold ("use case: stylized-concept, asset type: ...") even where told verbatim.
- **Evidence:** `.codex-tmp/session3/seed-r2/{seed-r2-a,b,c}.png`, `seed-r2-report.json`, `seed-r2-sheet.png`; `seed-light.mjs`.

### R059 — 2026-09-01 — Seed r2 round 2: flat lighting by words, mixed scenery (codex exec, gpt-5.6-sol, effort=high)

- **Setup:** same register; prompt demanded flat faces (ambient occlusion only) in strong language; scenery split into d mixed, e austere, f mixed + magical; reference = round-1 b. `53,813` tokens, 3 calls.
- **Verdict: scenery fixed, lighting unchanged.** d/e/f carry the austere vocabulary the owner asked for (bare plateau, scree, dark forest, moor, one lush pocket). Rock-only strong-edge moment `0.245 / 0.176 / 0.200 @ 43–58°` — no improvement at all, because the reference image carried the convention and the image wins over the words (F26 again, from the other side).
- **Evidence:** `seed-r2-{d,e,f}.png`, `seed-r2-report-round2.json`, `seed-r2-sheet-round2.png`.

### R060 — 2026-09-01 — Seed r2 round 3: the lit side removed at the source (codex exec, gpt-5.6-sol, effort=high)

- **Setup:** reference = the accepted quarry `c4-3-source.png` (neutral, `0.074`) for view, scale, density AND rock shading; the register by words only; g mixed with one natural wonder, h mixed + magical. `70,270` tokens, 2 calls.
- **Verdict: reference choice fixes what prompt words could not.** g: rock-only `0.105 @21°` — in the band of the current canon (`0.111`) and near the quarry; faces uniform by eye; the plateau top is pebbled ground rather than hexagonal paving. h: `0.246 @53°` — the glowing elements and the tilted column cluster bring the convention back (n only 20k strong rock edges; eyes needed before blaming the crystals). Owner's cross-cutting observation, confirmed: every generation shares one hand because one image model renders them all and every packet references the previous lineage; a prototype pipeline FINISH (`finish.mjs`: value steps, edge ink, split tone, seeded grain) was rendered on g as the cheapest lever toward "ours over the generator's". Owner parked the idea of mixing two agents' image tools (the director has none) and does not want a local model.
- **Workflow lessons banked:** (1) a gate calibrated on cast shadows (sunned D05) is blind to plane-shading keys; the rock-only strong-edge moment (threshold ~0.15) is the candidate gate, calibrated quarry 0.07 / canon 0.11 / lit candidates 0.18–0.27. (2) Shading convention travels through the reference image, not the prompt — pick references for what they must NOT teach as much as what they must. (3) Director-authored style words become defects when they name a convention ("cool shadow sides").
- **Sourced reference for the owner's "purple field" (director research, not memory):** Outward's purple is the mana-tinted ground around Conflux Mountain, Chersonese — patches on the lower slopes and along the corkscrew path, muted violet, diffusing into a wash, against grey rock and mist; lore: land soaked in raw mana turns purple like the mana stones. Sources: `https://outward.wiki.gg/wiki/Conflux_Mountain`, `https://outward.wiki.gg/wiki/Chersonese`, `https://steamcommunity.com/app/794260/discussions/0/4042609512655639922/`. Recorded in `scenery-variance-plan.md`.
- **Evidence:** `seed-r2-{g,h}.png`, `seed-r2-report-round3.json`, `seed-r2-sheet-round3.png`, `finish-compare-g.png`.

### R061 — 2026-09-01 — Regeneration 1/3: c4-3 quarry under canon r2, frontier cell (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** the L2 world wiped after the owner's register change; c4-3 baked first as a frontier cell in GENERATE mode with the new canon (`seed/L2-seed-region-r2.png` = round-3 candidate g) as the style reference and the continuation-style brief `brief-c4-3-r2.md` (quarry biome from the plan's biome map, austere scenery, flat lighting). `146,499` worker tokens, ~10 min, 2 ImageGen calls.
- **Verdict: ACCEPTED on all eight gates**, the new 48 px ring at `0%`, key-light `0.0017`; director's rock-only strong-edge moment `0.041` (flatter than the old quarry's `0.074`). `124` tiles. Eyes at 1/2.5 and 1:1: sea east and south with the shingle cove, stepped cut benches in pale rock, the flooded pit, the stream from the plateau over the cut face past the pit to the cove, conifers only on the west rim, meadow only as a thin rim, faces uniform.
- **Model-attributable observation:** the worker judged its first candidate's conifer crowns under the 41–72 px floor and used its second whole-canvas candidate rather than patching — the scale rule in the packet worked as a self-check. The mask was built from painted pixels (stream, fall foam, pit, sea, surf, wet shingle) and passed both rings at zero.
- **Evidence:** `.codex-tmp/session3/regen-c4-3.log`, `.codex-tmp/session3/review/regen1/c4-3-concept-{quarter,1to1}.png`, ledger record in the manifest (`biome: quarry`).

### R062 — 2026-09-01 — Regeneration 2/3: c3-3 saddle, the FIRST real edit-mode bake (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** the pipeline built `context/edit-target.png` (the new quarry's concept paint as a full-height strip on the east edge, grey elsewhere) and the packet mandated one built-in `image_gen` EDIT call with the verbatim square-framing preamble, the moor biome vocabulary and the transition line for the quarry neighbour. Brief `brief-c3-3-r2.md` (continuation, no coordinates). Two dispatch attempts died on "Selected model is at capacity" (0 and 19k tokens, nothing delivered); a director-side retry loop waited it out. Successful run: `188,671` tokens, ~9 min.
- **Verdict: ACCEPTED on all eight gates, and the seam no longer measures as a seam.** Palette at the quarry seam `dBG 0.021 / dLuma 0.9` (the old pair: `0.130 / 11.7`); fringe `0%` at both rings; key-light `0.0027`; rock-only strong-edge `0.056`. Real stitched seam along the ownership curve (`seam-step.mjs`): mean `20.3` / median `18.0` / p90 `40.7` against interior lines `16.4–23.3` — inside the no-seam range, where the old c3-3|c4-3 seam measured `21.8` against `12.6–18.3`. `143` tiles.
- **Model-attributable observation:** the worker read the edit target correctly ("the quarry neighbour already painted as a narrow full-height strip on the east edge... treating those pixels, their projection and their olive/gold-grey palette as binding") and produced a 1254 square on the first edit call. Capacity errors on `gpt-5.6-sol` at this hour are the operational risk; the pipeline should retry them itself (next lock change).
- **Evidence:** `.codex-tmp/session3/regen-c3-3.log`, `.codex-tmp/session3/review/regen2/{seam-33-43-1to1-*,mounted-quarter}.png`, `review/seam-diag/seam-33-43-sourcemap-half.png` (re-run on the new world), ledger record (`biome: moor`).

### R063 — 2026-09-01 — Regeneration 3/3: c4-2 coast, edit mode from two neighbours (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** the first two-neighbour edit target (quarry paint across the south band with its stream entering and the sea at the east, the saddle's moor in the south-west corner, grey elsewhere); coast-cliff biome; continuation brief `brief-c4-2-r2.md` with no coordinates. `169,510` tokens, ~10 min, one edit call.
- **Verdict: a GOOD candidate rejected by a gate artifact.** Seven of eight gates PASS (both fringe rings `0%`, key-light `0.0021`, palette n/a). Water continuity FAILED: "my crossing at 8589 unmet, nearest 1561px". Measured: on the exact shared row the quarry's stream is split by one boulder into runs of `10` and `17` px, both under the gate's `30` px minimum (one row above `37` px, one below `33` px), so the gate read no quarry crossing at all; the coast's stream meets the quarry's at a `25–45` px miss, inside the `48` px tolerance. Eyes on the stacked crop: the channels align and the candidate continued the quarry's stones and its edge conifer — the strongest evidence yet that edit mode continues real pixels. A control reproducing the defect (a 5-column stone straddling the shared line for half the stream's rows) was added to the suite and FAILS on the current gate exactly as the world did.
- **Model-attributable observation:** the worker's own mask discipline improved under the new rule: it rejected its first threshold mask for dropping the rocky stream between pools and rebuilt it to follow the painted signal — both ring gates at `0%`.
- **Workflow lesson banked:** a gate that samples one scanline of a natural channel is measuring the stones, not the water — the void-gate pattern again. Fix pending owner approval: read crossings over a band across the line.
- **Evidence:** `.codex-tmp/session3/regen-c4-2.log`, `.codex-tmp/session3/review/regen3-coast-seam-check.png`, the control in `tests/world-authoring-stitch.test.mjs`.

### R064 — 2026-09-01 — Regeneration 3/3 (second attempt): c4-2 coast, edit mode, weathered-cliff brief (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** the owner rejected the first coast candidate ("the cliff side is unnatural": identical columns in a straight palisade with a square corner). The coast-cliff biome vocabulary and the geology rule now demand weathered, irregular columns, and the brief carries it with the ledge made explicit; the continuity gate now reads a band across the shared line (the first candidate had been rejected on a single-scanline artifact). Same two-neighbour edit target. `163,598` tokens, ~10 min, one edit call.
- **Verdict: ACCEPTED on all eight gates.** Continuity `ok (4 crossings checked)` — the quarry's stone-split stream matched through the band; fringe `0.09% / 0.05%`; key-light `0.0013`; rock-only strong-edge `0.057`. `145` tiles. Real stitched seams (`seam-step.mjs`): c4-2|c4-3 `20.8` mean step against interior `18.1–25.3` (old world `26.0`); c3-3|c4-3 unchanged at `20.3`. The three-cell world is complete under canon r2.
- **Model-attributable observation:** with the vocabulary naming what a weathered column looks like, the worker painted uneven heights, broken tops and collapsed drums on the first try; the earlier tidy palisade was the model's default filling a gap in the brief, not a limit of the model.
- **Evidence:** `.codex-tmp/session3/regen-c4-2.log`, `.codex-tmp/session3/review/regen3/` (sheets 1–5 with the old world as "before"), `c4-2-concept-quarter.png`, `c4-2-cliff-1to1.png`; the rejected candidate kept at `.codex-tmp/session3/rejected/c4-2-cliff/`.

### R065 — 2026-09-01 — c3-2 dark-forest gorge, first attempt: the first three-neighbour edit target (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** the first cell with an L-shaped painted arrival (coast on the east band with its tarn and fall country, saddle on the south band, quarry at the corner); dark-forest biome vocabulary; sites hermit's crag and giant tree in the packet; the runner's own capacity retry (not needed, one attempt); stale deliverables cleared at dispatch by the pipeline. `159,173` tokens, ~10 min.
- **Verdict: REJECTED by the continuity gate, correctly.** The gorge stream left through the SOUTH edge at world x `7540` where the saddle's north edge is dry, so nothing meets it — a brief violation, not a gate artifact (the band read passed the quarry seams in the same run). Every other gate passed: fringe `0% / 0.05%`, key-light `0.0067`, rock lighting `0.108` report-only at `3446` strong rock edges (a forest cell, as designed). The worker reported the defect itself in `open_risks` ("the generated gorge stream visibly joins the pre-painted saddle beck and therefore reaches the south edge"), plus underscale conifer crowns and a rectilinear crag tabletop — all three confirmed by eye on the candidate. Candidate kept at `.codex-tmp/session3/rejected/c3-2-south-exit/`.
- **Model-attributable observation:** sol continued the arriving paint on both bands and delivered the biome (darkest green, mist in the gorge, one clearly oversized conifer) on the first edit; it joined the visible saddle beck because the brief said "must NOT reach the south edge" in one clause while the arriving paint offered a beck to join — the image won over the clause again. The re-brief states the water as a CLOSED system and names the dry south edge as unmet by construction.
- **Workflow lesson banked:** in edit mode, write water topology as what the arriving paint permits (which edges carry water, which are dry), not as a prohibition; the model follows the paint.
- **Evidence:** `.codex-tmp/session3/regen-c3-2.log`, `.codex-tmp/session3/review/regen-c3-2-cand1-quarter.png`, the worker's `c3-2-report.json` (rejected copy).

### R066 — 2026-09-01 — c3-2 dark-forest gorge, second attempt: accepted (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** same three-neighbour edit target; the brief restated the water as a CLOSED system with the dry south edge named as unmet by construction, added the crown-scale line tied to the arriving paint, and asked for a weathered, irregular crag. `203,362` tokens, ~11 min, one edit call.
- **Verdict: ACCEPTED on all nine gates** — continuity `ok (2 crossings checked)`, fringe `0.14% / 0.02%`, key-light `0.0033`, **rock lighting `0.052` gated** (enough strong rock edges this time), land `96.4%`. `168` tiles (the widest dirty set so far: three neighbours' seams re-derived).
- **Model-attributable observation:** stating topology as permission ("this cell's water is closed; the south edge is dry") worked where a prohibition did not; the worker kept the stream inside the cell on the first edit. The environment noise continues (it probed for ImageMagick, then triaged) without affecting the deliverable.
- **Evidence:** `.codex-tmp/session3/regen-c3-2.log`, `.codex-tmp/session3/review/regen-c3-2-quarter.png`, ledger record (`biome: dark-forest`).

### R067 — 2026-09-01 — c2-3 border moor, edit mode from the saddle (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** moor biome on the land border into Tanium (owner ruling: south edge solid ground); site border-knoll; the saddle's paint arriving on the east band and the gorge cell at the corner; brief `brief-c2-3-r2.md` with the water stated as one beck leaving south and every other edge dry. `175,757` tokens, ~11 min, one edit call.
- **Verdict: ACCEPTED on all nine gates** on the first attempt — continuity `ok`, fringe `0% / 0%`, key-light `0.0036`, rock lighting `0.131` report-only at `3289` strong rock edges (a moor cell, little rock), land `98.3%`. `140` tiles.
- **Model-attributable observation:** stating the water as permission worked again on the first try. The worker flagged its own inherited defect honestly: the column wall arriving from the saddle's east side "continues as a long, fairly regular column wall" — the saddle was painted before the weathered-column rule and the edit continued what arrived. The rule now lives in the biome vocabulary, so cells painted from here on carry it; the saddle itself is a candidate for a later regeneration under the rule if the owner's eye objects.
- **Evidence:** `.codex-tmp/session3/regen-c2-3.log`, `.codex-tmp/session3/review/regen-c2-3-quarter.png`, ledger record (`biome: moor`).

### R068 — 2026-09-01 — c3-1 magical gorge, first attempt: two measured rejections on the best-looking cell yet (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** the rail gorge-span moment; magical-gorge biome (floating fragments, self-luminous crystals, glowing pool); 3,2's forest and gorge arriving on the south band, the coast at the corner; brief with the scale line. `211,518` tokens, ~12 min, one edit call.
- **Verdict: REJECTED by two gates, both correctly.** (1) **Rock lighting `0.31`** (limit `0.15`, `5711` strong rock edges): direction `33°` with half the strong-edge energy in two adjacent 45° bins, identical with the violet crystal pixels excluded — column-by-column cylinder shading with the same bright side on every column, an implied light, subtle at 1:1 and real. (2) **Water continuity**: the coast's tarn runs `40–60 px` north of the coast's kept edge into the row above; 3,2's edit carried that shore to the corner; the 3,1 edit target showed the tarn's sliver ABOVE the line inside the candidate's own kept corner (rows 2280–2310, world 4072–4102), and the candidate painted it over as land, so the `99 px` crossing on 3,2's side was unmet. The worker also reported its side stream reaching the east edge for 16 px and the fall-side wall reading regular. Everything else passed (fringe `0.02% / 0%`, key-light `0.0016`, palette `dBG 0.145`). Candidate kept at `.codex-tmp/session3/rejected/c3-1-cylinders/`.
- **What the cell got right:** the chasm with flat mist, hanging column fragments trailing roots and moss with crystals on them, the great fall into a teal pool, dark conifers gathering at the rims — the owner's crop realised. The re-brief changes only the two measured things: every column face one flat value (joints as thin dark lines, occlusion only), and the tarn corner stated as permission with the mask told to cover arriving water.
- **Contract note (no pipeline change yet):** a neighbour's water body whose shore crosses a cell line is legitimate and the edit target shows it; the later cell must continue it. If a worker fails this again, the robust fix is the pipeline ORing the neighbours' water into the delivered mask within the bleed, which makes the gate pass but would leave a straight shore on the ownership curve — so the brief-level fix is the right first fix.
- **Evidence:** `.codex-tmp/session3/regen-c3-1.log`, `.codex-tmp/session3/review/regen-c3-1-cand1-{quarter,chasm-1to1,SEcorner}.png`, `regen-c3-1-target-SEcorner.png`.

### R069 — 2026-09-01 — c3-1 magical gorge, second attempt: both fixes held, a third gate fires (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** brief revised for flat column faces and the tarn corner stated as permission. `159,832` tokens, ~11 min, one edit call.
- **Verdict: REJECTED by the palette gate only** — `dBG 0.218` at the 3,2 seam (limit `0.18`, dLuma `2.8`); rock lighting `0.143` (from `0.31`), continuity `ok (2 crossings)` with the tarn corner continued, fringe `0.35% / 0.41%`. Gate replica on the two bands: 3,2's north band vegetation blue/green `0.681` (its gorge top lies under flat haze), the candidate's south band `0.463` (clear forest) — the haze stops at the cell line. Eyes on the stacked seam band: a mild but findable warm-to-cool step; the trees themselves match. The candidate otherwise realises the brief and is kept in full (source, derived concept/l2/water, report) at `.codex-tmp/session3/rejected/c3-1-palette/` so it can be accepted through `--from` if the owner judges the step acceptable. Also noted by eye: crystals scattered across the open moor rather than confined to the rims and fragments.
- **Model-attributable observation:** each attempt fixed exactly what it was told and surfaced the next unstated thing — cylinder shading, then the corner, then the haze. The pattern is the edit model continuing STRUCTURE from the arriving band reliably and ATMOSPHERE (mist tint) only when told; the haze sentence now lives in the brief, and belongs in the dark-forest and magical-gorge biome vocabularies for the next lock change ("haze continues across seams at the arriving tint").
- **Evidence:** `.codex-tmp/session3/regen-c3-1.log`, `.codex-tmp/session3/review/regen-c3-1-cand2-{quarter,seamband,seamband-1to1}.png`.

### R070 — 2026-09-01 — c2-2 bench country, first attempt: the rock-lighting gate on a biome made of walls (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** mixed-bench biome; sites waystation bench and hot spring; three neighbours arriving (gorge cell east, border moor south, saddle corner); brief with closed water and the scale line. `163,816` tokens, ~12 min, one edit call.
- **Verdict: REJECTED by the rock-lighting gate alone** — `0.20` against `0.15`; everything else green (palette `dBG 0.167`, fringe `0% / 0%`, continuity ok). The worker's report names the cause before the gate did: "long, repeated column runs and several regular terrace corners", the cylinder-shading habit on a biome built from column walls. It also flagged the steam haze as weak. Candidate kept at `.codex-tmp/session3/rejected/c2-2-palisade/`.
- **Model-attributable observation:** the flat-face instruction that fixed 3,1's second attempt (`0.31 → 0.143`) was not in this brief; the vocabulary alone ("weathered, irregular") did not stop cylinder shading. The instruction now lives in the brief; it belongs in the geology rule for the next lock change so every packet carries "every column face one flat value" verbatim.
- **Evidence:** `.codex-tmp/session3/regen-c2-2.log`, `.codex-tmp/session3/review/regen-c2-2-cand1-quarter.png`.

### R071 — 2026-09-01 — c3-1 magical gorge, third attempt: the closest of three, rejected by the mask and a 5% palette overshoot (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** `brief-c3-1-r2.md` as re-written after the owner's seam ruling (haze carried across the south third at the arriving tint, crystals confined, every rock form continuing at the arriving size); edit mode with c3-2 (south) and c4-2 (east) in the target; `204,723` tokens, 12.9 min, one edit call at 1254 px.
- **Verdict: REJECTED by two gates.** Water fringe `4.9%` (6 px) / `2.49%` (48 px) against `<1%`; palette `dBG 0.189` against `0.18` at the c3-2 seam (attempt 2: `0.218`). Rock lighting `0.116` — the lowest of the three attempts (`0.31 → 0.143 → 0.116`), so the flat-face instruction holds. Continuity ok (2 crossings), the tarn corner continued.
- **Where the fringe is** (`fringe-map.mjs`, replicates the gate to the pixel: `4.90% / 2.49%`): of 3,483 uncovered water-coloured pixels within 48 px of the mask, 2,026 lie on the tarn's south shore in the SE bleed and about 1,000 on the west shore of the luminous pool below the fall — the mask stops short of the painted water in two places. The worker's own proxy found the same (`5.7% / 2.8%`) and excused it as "cyan-leaning conifer and crystal pixels"; the map says otherwise. Mask tracing, not art.
- **What the palette number is** (`haze-profile.mjs`, vegetation-median b/g by 64 px band from the seam): 3,2's north band `0.65 / 0.73 / 0.61 / 0.61 …` (gate band `0.681`); the candidate's south band `0.57 / 0.48` for its first 128 px, then no vegetation for 128 px (rock and tarn), then `0.74 / 0.87 / 0.91 …` — the candidate continues the arriving tint for about 128 px and then paints its own, far colder cell (whole-cell `0.93` against 3,2's `0.61`, the quarry's `0.40`, the moor's `0.57`). Attempt 2 had the same shape (`0.67 / 0.43 / 0.38 … 0.82`). The gate's band sits on the continued part, so the number is a 5% miss; the step the eye finds is further in.
- **Crown scale:** the worker measured its median crown at `49 px` final (`24` source px) against the `84 px` floor and reported it. A director measure applied identically to both sides (`crown-size.mjs`: dark-conifer blobs of 300–20,000 px in a 400 px band) gives 3,2's north band `66 px` median (n 10) against the candidate's south band `61 px` (n 18) — a 0.92 ratio, not 0.5; both measures are biased (merged canopy drops out of mine; the worker's is visual). Inconclusive, not a verdict either way. A crown gate needs a better detector before it can gate.
- **Eyes at 1:1 on the composed south seam** (`review/regen-c3-1-cand3-seam-S-{west,east}.png`, candidate above the line, 3,2 below): the crag and the rim wall cross the line without a scale change this time; the join is findable as a texture and tone line through the moor and the forest, not a structural clash. Quarter view `regen-c3-1-cand3-quarter.png`: the chasm continues 3,2's gorge; fragments, crystals, fall and glowing pool as briefed.
- **Model-attributable:** three attempts, three different first failures (lighting → haze → mask + palette); each brief fix landed and the next constraint slipped. With about six independent constraints at 70–80% each, one-shot acceptance is a coin toss — the constraints that can be enforced by construction (mask coverage, seam tone) should leave the brief.
- **Evidence:** `.codex-tmp/session3/regen-c3-1.log` (after `rebake3-start`); candidate kept in full at `.codex-tmp/session3/rejected/c3-1-cand3/` and still in place in `.codex-tmp/authoring/cells/c3-1/`; `review/regen-c3-1-cand3-fringe-map.png`.

### R072 — 2026-09-01 — c2-2 bench country, second attempt: flat faces landed, the palisade did not leave (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** `brief-c2-2-r2.md` — flat column faces, broken walls, legible steam; edit mode with c3-2 (east), c2-3 (south), c3-3 (corner). 12.8 min; the worker log was overwritten by the third dispatch before its token line was captured.
- **Verdict: REJECTED by the palette gate alone** — `dBG 0.266` at the c3-2 seam (attempt 1: `0.167`, a pass). Fringe `0% / 0%`, continuity ok, rock lighting `0.513` on only `2,672` strong rock edges (report-only under 5,000): the flat faces removed most strong rock edges, and the few left sit on the terrace tops.
- **The number:** the candidate's east-band vegetation reads `0.29` b/g — a warm yellow-green — against the arriving forest's `0.56`; the rest of the cell has almost no pixels the gate calls vegetation (heather, scree, pale rock: fewer than 500 in the whole kept area at an 8 px stride). Attempt 1 had been pale and cold instead (`0.91` whole-cell, luma about 100). The edit model's palette swings between attempts of the same brief.
- **The owner's defect is still there, by the worker's own report** ("long repeated column runs and regular terrace arcs … visually close to the owner-rejected palisade defect") and by eye at quarter zoom (`review/regen-c2-2-cand2-quarter.png`): continuous terrace walls hundreds of px long, one U-shaped run with a square corner. Two attempts, same defect. **Diagnosis:** the biome's own words ("stepped benches and terraces … column walls between benches") describe continuous walls; "never a long straight run" contradicts the landform it asks for, and the landform wins. **Reframe (third attempt, `brief-c2-2-r3.md`):** broken bench country — heather flats in shallow steps read from scree fans, ramps and SCATTERED outcrops (tors, knolls, short broken scarps two to six crowns long), no continuous edge anywhere; the arriving dark cool green held across the whole east third. Vocabulary change queued for the lock change.
- **Evidence:** `.codex-tmp/session3/regen-c2-2.log` (after `rebake2-start`); candidate at `.codex-tmp/session3/rejected/c2-2-cand2/`.

### R073 — 2026-09-02 — The control suite shares the real working directories: a test stub reached cell 4,3's ledger record (director forensics, no model)

- **Found** while checking the worktree: `git status` showed `art-source/…/c4-3/c4-3-water.json` modified — its content was `[{"class":"lake","note":"synthetic control"}]`, the suite's stub. The ledger record for c4-3 said `waterZones: ["lake"]` instead of `[coast, lake, stream, fall]`.
- **Mechanism:** `cell.mjs` relocates sources, tiles and manifest under `L2_OUT_ROOT` for the suite, but `WORK = ".codex-tmp/authoring/cells"` is not relocated. The rock-lighting control supplies cell `4,3` via `--from`, which copies l2/concept/water/water.json into `WORK/c4-3` — the real cell's working dir (20:07:06). The `--redo --force` re-record of 4,3 under lock change 4 (20:09:43) re-derived from the untouched source, read the stub as the zones, and copied it into art-source. Only the record was wrong: zones feed nothing pixel-side (the tiles were byte-identical then and now).
- **Fix:** restored both copies from git, re-ran `--redo --force 4,3` with every L0–L6 tile hashed before and after: `0 written, 124 byte-identical`, hash diff empty, record now `[coast, lake, stream, fall]`. Committed `97eb0d6`.
- **Lock-change item:** relocate `WORK` under `L2_OUT_ROOT` (the suite must never touch the real working dirs — the stale-deliverable control also wipes `WORK/c3-2`'s deliverables today), plus a suite assertion that nothing under `.codex-tmp/authoring/cells` changes during a run.

### R074 — 2026-09-02 — c2-2 broken bench country, third attempt: the landform reframed, accepted on all nine gates (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** `brief-c2-2-r3.md` — the biome's structure re-described as broken bench country (heather flats in shallow steps read from scree fans, ramps and SCATTERED outcrops: tors, knolls, short broken scarps two to six crowns long; no continuous edge), the arriving cool green held across the whole east third, flat column faces, the mask self-check line. Edit mode with c3-2 (east), c2-3 (south), c3-3 (corner). `387,569` tokens (the worker's heaviest run so far), 12.5 min, one edit call at 1254 px.
- **Verdict: ACCEPTED** — key-light `0.0108` (limit `0.011`, the closest pass yet), rock lighting `0.245` on `874` strong rock edges (report-only under 5,000: the flat faces leave almost no strong rock edges), land `97.3%`, cut `0 px`, fringe `0% / 0%`, continuity ok (0 crossings — closed water as briefed), palette `dBG 0.143 / dLuma 0.3` at the 3,2 seam (attempts 1 and 2: `0.167`, `0.266`). `162` tiles written, 5 byte-identical. Committed `f1bdf80`.
- **Eyes on the stitched tiles** (`review/stitched-c2-2-quarter.png`, seam halves `stitched-c2-2-seam-{E,S}-*.png` at 1:1): the terrace walls are gone — the moor is studded with short weathered tors; the hot spring with pale rims (steam mostly cut with the pool's footprint); the beck and its pool. Both seams read continuous at 1:1: trees straddle the gorge line, the moor-to-moor line to 2,3 is not findable. **One item for the owner's eye** (`review/stitched-c2-2-east-scarp-1to1.png`): a long WANDERING escarpment of broken column stubs bounds the forest bench through the east third — uneven, broken, no square corner, but one connected edge about 1,300 px long, over the brief's six crowns; the worker flagged it itself.
- **What the reframe shows:** two attempts on "never a long straight run" against a biome described as terrace walls produced walls; describing a different landform produced it. The landform words go into the vocabulary (lock change 5d) — the negative rule alone did not land.
- **Watch item, again:** the worker measured its median crown at `57 px` final against the `84` floor (chain: `71 → 49? → 57`); every brief carries the scale line and the crowns keep coming in under it. Report-only crown measure in lock change 5e.
- **Evidence:** `.codex-tmp/session3/regen-c2-2.log` (after `rebake3-start`), `.codex-tmp/authoring/cells/c2-2/c2-2-report.json`.

### R075 — 2026-09-02 — c1-2 heather moor, first attempt: the key-light gate fires on a real bake, and the light is inherited (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** `brief-c1-2.md` — moor with the hamlet clearing and the lookout crag, closed water, weathered rock, the mask self-check line; edit mode with c2-2 (east) and c2-3 (corner). `173,696` tokens, 13.1 min, one edit call at 1254 px.
- **Verdict: REJECTED by the key-light gate alone** — `0.021` against `0.011` (a baked sun measures about `0.03`); every other gate green (fringe `0/0%`, land `98.3%`, palette "no vegetated seams": the moor has too few pixels the gate calls vegetation). Rock lighting `0.630` on `56` strong rock edges, report-only. The worker's own proxy passed (`aggregate_directional_key: true`) — its metric is not the gate's.
- **Where the light is** (`keylight-map.mjs`, the gate's moment per 256 px block with direction): block mean luma is flat (`61–80`, one pale block = the clearing), so it is not a tonal ramp; the moment is `0.02–0.05` in nearly every block, all pointing at `0–60°` (global `0.0205 @ 33°`). Eyes at 1:1 (`review/regen-c1-2-cand1-tors-1to1.png`): every heather tussock has a bright upper edge and a dark lower edge, every boulder a bright top and a dark side, the beck a pale bank on one side — an emboss, the same way up everywhere.
- **The light is inherited and compounding.** The same measure along the authoring chain: seed canon `0.0008` → 4,3 `0.0026` → 3,3 `0.0035` → 4,2 `0.0024` → 3,2 `0.0052 @ 14°` → 2,2 `0.0121 @ 34°` (the gate read `0.0108`, a pass with no headroom) → 1,2 `0.0205 @ 33°`. From 3,2 on the direction locks and the magnitude roughly doubles per edit-mode generation: the edit target is the only image the model sees, so each cell continues its neighbour's hand and adds its own drift. The canon never reaches the image model in edit mode. Same mechanism as the crown drift (`71 → 57 → 45 px`).
- **Other worker-reported misses:** the hamlet clearing at 15–18 m against 30 m; no unmistakable flat-topped lookout crag; tors as regular upright bundles; crowns `45 px` final against the `84` floor.
- **Model-attributable, and the experiment queued:** words moved the rock-only moment on 3,1 (`0.31 → 0.143`) when the instruction was concrete. Attempt 2 (`brief-c1-2-r2.md`) names the emboss exactly (no bright upper edge, no dark lower edge on any tussock; boulders the same value on every side; both banks the same value) with size anchors in crowns. If the global moment does not move, the lineage needs a pipeline anchor, not a brief (lock change 5h).
- **Evidence:** `.codex-tmp/session3/regen-c1-2.log`, candidate at `.codex-tmp/session3/rejected/c1-2-cand1/`, `review/regen-c1-2-cand1-quarter.png`.

### R076 — 2026-09-02 — c1-2 heather moor, second attempt: concrete words do not move an inherited key light (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** `brief-c1-2-r2.md` — the emboss named exactly (no bright upper edge, no dark lower edge on any tussock; boulders the same value on every side; both banks the same value), size anchors in crowns, crowns tied to the LARGEST arriving ones. One capacity refusal retried by the runner (120 s). `210,514` tokens, one edit call at 1254 px.
- **Verdict: REJECTED by the key-light gate again** — `0.0186` against `0.011` (attempt 1: `0.021`); `keylight-map.mjs`: global `0.0188 @ 36°` (attempt 1: `0.0205 @ 33°`). Every other gate green. The worker's own proxy passed again (`aggregate_directional_key_proxy: true`) and it reported `equal_value_on_all_terrain_faces: false` itself.
- **The experiment's answer:** the words that moved the rock-only moment on 3,1 (`0.31 → 0.143`, a column-shading habit) moved the global moment here by a tenth and left the direction untouched. The emboss is the arriving neighbour's hand (2,2 at `0.0121 @ 34°`), continued and amplified; in edit mode the edit target is the only image the model sees, so no prompt reaches past it. Eyes at 1:1 (`review/regen-c1-2-cand2-tors-1to1.png`): the same tussock emboss, and the "low outcrop sheltering the clearing" came out as a regular fence of identical columns with a square corner — the owner's rejected geology, again.
- **What improved:** crowns `69 px` final (attempt 1: `45`) once the brief tied them to the largest arriving crowns; the clearing width and the lookout crag landed by the worker's report.
- **Two strikes → no third bake under the same conditions.** The lever is a pipeline anchor (lock change 5h): (i) the canon as a second input image in the same edit call, if the built-in tool accepts two images — a packet-text change only; (ii) failing that, the canon's paint ghosted at low opacity into the target's grey "to paint" area so the hand the model continues is the canon's in the interior and the neighbour's at the band. Either needs one owner-authorised probe run outside the pipeline (as R056/R057 were), measuring the moment and the seam fidelity (`r` against the neighbour's band; the pipeline's own path measured `0.74`).
- **Evidence:** `.codex-tmp/session3/regen-c1-2.log` (after `rebake2-start`), candidate at `.codex-tmp/session3/rejected/c1-2-cand2/`, `review/regen-c1-2-cand2-quarter.png`.

### R077 — 2026-09-02 — Lock change 5, part 1: the suite gets its own working dir, the mask completes itself, the palette limit moves to the owner's eye (director, controls first)

- **Owner ruling (in chat):** on 3,1 candidate 3, rejected by the fringe gates (`4.9% / 2.49%`) and the palette gate (`0.189` vs `0.18`): "youre being way too harsh I think, thats a tiny fix no? no need for a full reject just fix that water issue? all of these looks pretty great imo", and on the fall-and-pool crop: "the water falls into a cavern perfectly fine". The lock record was the owner's keystroke by procedure; his own run from the main checkout failed (the pipeline dir lives on the lane branch), and he told the director to record it — the record quotes him.
- **(a) WORK follows `L2_OUT_ROOT`.** The suite's `--from` and dispatch runs wrote into the real cells' working dirs (R073: a stub reached 4,3's ledger record; a suite run during a live bake would have wiped candidate 3's deliverables and the bake). Now `<OUT>/work`; the suite fingerprints the real dirs at load (names, sizes, mtimes; a cell touched in the last hour is a live bake and is skipped) and asserts them unchanged at the end.
- **(b) Mask completion by bounded growth,** before the boundary feather and the cut: the delivered mask grows into 8-connected opaque pixels the fringe classifier calls water (`mx>40, sat>.25, b>r, b>=g`) up to 48 px from the mask; dry 4-connected holes of ≤64 px enclosed by water fill; bridge bands are exempt (the bridge knowingly orphans the old channel's paint). Reported as a gate-table row (`+N px grown, M px holes filled`) and a ledger field `maskCompletion`. Dry paint stops the growth, so the polyline-mask defect still fails the 48 px ring. Prototype on the five accepted cells: `0 / 0 / 103 / 9 / 0` opaque px.
- **(c) Palette limit `0.18 → 0.20`,** on the calibration the gate was built on — the owner's eye: fine at `0.109` and `0.189`, clash at `0.218` ("close but a few seam issues") and `0.294`.
- **Controls (suite `16/16`):** an undersize disc mask (20 px short of the painted shore) completes (`+N` within ±15% of the annulus area) and both rings read `0%`, the shore pixel is cut in the tile; a dry strip between the mask and painted water leaves the 48 px ring failing and the world untouched; a flat blue-grey haze (`sat 0.12`) beside the water is not absorbed and stays land; the real working dirs are untouched by the whole suite. Hash gate intact; lock history has the before and after entries; commit `6f2e14c`.

### R078 — 2026-09-02 — Lineage probe: the canon as a second input image to the same edit call halves the inherited key light (owner-authorised probe, one codex job, gpt-5.6-sol)

- **Question (R075/R076):** in edit mode the model sees only the edit target, so a neighbour's hand compounds along the chain (key light `0.005 → 0.012 → 0.021`, crowns `71 → 57 → 45 px`), and concrete words did not move it. Does the built-in `image_gen` EDIT call accept a second input image, and does the canon as that reference flatten the light without breaking the seam?
- **Setup:** `.codex-tmp/session3/probe-anchor/` — cell 1,2's real edit target plus the canon r2 source (1254 px) as a second input; the framing paragraph extended with "the second image is a reference only … take from it the brush, the palette family, the ground scale and above all the FLAT LIGHTING"; the 1,2 brief in short. One call, `40,295` tokens.
- **Tool truth:** both images accepted in one edit call, `1254x1254` returned, raw PNG delivered.
- **Measured** (`keylight-map.mjs`, `band-fidelity.mjs`, source upscaled to 2560 as the pipeline does): global key-light moment **`0.0093`** (under the `0.011` gate) against `0.0203` and `0.0194` for the two pipeline attempts from the same target; block map scattered (`0.005–0.033`, mixed directions) where the attempts read `0.02–0.05` all at `0–60°`. Seam fidelity to the arriving band (Pearson r of 4x-downsampled luma over the neighbour's 512 px): **`0.759`** against `0.877` / `0.839` — the reference pulls the band slightly toward itself, but it stays at the level the edit path was adopted on (`0.74`, R057) and far from generate mode (`~0`).
- **Eyes** (`probe-anchor/probe-source.png`): moor at the right scale, tors continuing the arriving bench country, the tussock emboss much reduced; content drifted where the probe brief was thin (a forest along the west, a beck leaving through two edges) — a probe, not a candidate.
- **Model-attributable:** the hand travels through the images the model is given, and it can be given two. One sample; the real bake's gates are the replication. Proposed as lock change 5 part 2: the packet mandates the canon source as the second input of the single edit call with the reference paragraph verbatim; control: the dry-run packet asserts it and the canon file exists.

### R079 — 2026-09-02 — c3-1 magical gorge, candidate 3 ACCEPTED on the owner's ruling after mask completion (pipeline `--redo`, no new generation)

- **Run:** `--redo` from the same deliverables (`source` sha `04fa70a6…`, mask sha `b2afdd5b…`) under lock change 5 part 1. Mask completion `+3,880 px` grown, `58 px` holes; fringe `0.54% / 0.89%` (from `4.9% / 2.49%`); palette `dBG 0.189 / dLuma 4.6` under `0.20`; key-light `0.0033`; rock lighting `0.116`; continuity ok (2 crossings, the tarn corner continued); land `97.4%`. `156` tiles written, 12 byte-identical. Committed `272f614`. **Seven cells stand.**
- **Eyes on the stitched tiles** (`review/stitched-c3-1-quarter.png`, `stitched-c3-1-seam-S-{west,east}.png`): the chasm continues the gorge, the fall and glowing pool cut with the completed mask, the tarn corner cut; at the real ownership curve the forest and the rim wall cross without a findable line — the composite's straight-line step in R071 was the preview's hard cut, not the stitch.
- **Watch items carried:** crown scale (worker `49 px` vs my `61` band median — inconclusive), the cold interior tint (`0.93` whole-cell) that no gate measures and the owner's eye accepted.

### R080 — 2026-09-02 — Lock change 5, part 2: the canon rides along as the second input of every edit call (director, on the owner's word; controls first)

- **Owner:** "sure you can make it the default if its been working. The images and seams look good to me!" — the second sentence also closes the seam ruling on 3,1 (checklist 13) and accepts 2,2's east-third escarpment.
- **Change (`cell.mjs`):** the edit-mode packet mandates BOTH images in the single `image_gen` edit call — the edit target as the image to edit, the style canon source (`seed/L2-seed-region-r2-source.png` under the sources root) as a reference-only second input; the framing paragraph now says "the first image"; a verbatim `REFERENCE_PARAGRAPH` follows it (take from the second image the brush, the palette family, the ground scale and above all the flat lighting: no lit side anywhere, ambient occlusion only); edit mode refuses to build without the canon file; a tool refusal means "report and stop — never a second call, never generate mode".
- **Basis:** probe R078 — one sample: key-light moment `0.0093` (under the `0.011` gate) against `0.019–0.020` for the two pipeline attempts from the same target; seam-band fidelity `r 0.759` against `0.84–0.88` (the edit path was adopted at `0.74`). The real bakes' gates are the replication; 1,2's third attempt is the first.
- **Controls (suite `16/16`):** the dry-run packet for an edit-mode cell asserts "BOTH images attached", the reference paragraph and the canon path; the test world carries a synthetic canon under its sources root. Honest note: the first application placed the canon-path check above the constant it reads (a temporal dead zone) and every control failed at module load — the check moved below the constants, suite green, and only then the after-record. Lock history has both records; hash gate intact; commit `61f0dd7`.
- **Also this session:** the territory grid status render (`grid-status.mjs` → `review/territory-grid-status.png`): 7 of 20 cells stitched (the south-east block, stitch order 1–7), planned biome and shelf on every unauthored cell.

### R081 — 2026-09-02 — c1-2 heather moor, third attempt: the canon as the second input replicates on a real bake; eight cells stand; the pyramid checked offline (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** `brief-c1-2-r2.md` unchanged from attempt 2; the only difference is lock change 5 part 2 — the packet mandated the canon source as the second input of the single edit call with the reference paragraph. The worker confirms `mode: "built-in imagegen edit with edit target and canon reference"`, one call, no retry. `273,524` tokens.
- **Verdict: ACCEPTED on all ten gates** — key-light `0.0068` against `0.011` (attempts 1 and 2 on the same edit target: `0.021`, `0.0186`; the probe R078: `0.0093`); rock lighting `0.337` on `1,309` strong edges (report-only); fringe `0% / 0%`; mask completion `+0 px` (the delivered mask was complete); continuity ok, closed as briefed; palette "no vegetated seams" (moor). `167` tiles. Committed `ee81a13`. **EIGHT CELLS STAND.** Director measure on the stitched l2: `0.0056 @ 33°` — the direction is still the inherited one, at a quarter of the strength.
- **Eyes** (`review/stitched-c1-2-quarter.png`, `stitched-c1-2-tors-1to1.png`): the hamlet clearing ten crowns wide beside the beck, the lookout crag on the south side as the tallest rock with a flat top, tors scattered and broken; the tussock emboss faint at 1:1; the beck long with more pools than the brief's two or three (worker-reported). Worker-reported crowns `49 px` final against the `84` floor — the crown drift persists and is the next thing to anchor (lock change 5e report-only first).
- **Model-attributable:** the hand travels through the images the model is given; giving it the canon beside the neighbour halves the inherited light in one shot, on a probe and on a real bake. The mechanism, not the words, was the lever (R075/R076).
- **Offline LoD check (owner asked whether the pyramid works before more cells are spent):** `pyramid-sheet.mjs` → `review/pyramid-A-block-L3-L6.png` (the 7-cell block at L3, L4, L5, L6 native) and `pyramid-B2-seam-stretched.png` (one 512 px window with the 3,3|4,3 seam down its centre at L0 native and L1–L3 stretched back with nearest sampling): the seam is not findable at any level, the brush character survives to L3, the tone holds across tiers. The reduction arithmetic is the suite's control. **Untested:** the runtime — the app has never streamed this pyramid; its land streamer takes a manifest of tiles with normalized world bounds and two source tiers (capital, site) across five camera tiers with fixed cross-fade spans. Wiring the pyramid in for a stripped dev preview (manifest generator, a streamer extension for more tiers, a layer-only toggle, a dev placement from NinjaOne's focus view) is app work, about half a day, awaiting the owner's word.
- **Grid:** `review/territory-grid-status.png` — 8 of 20.

### R082 — 2026-09-02 — c2-1 the capital's shelf, first attempt: every gate green, the shelf itself did not land (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** `brief-c2-1.md` — the capital's shelf a third of the cell, luminous gold-green with glades and orchard stands, held above the country by low broken terraces, a station strip along its south side, the line's ground west-to-east, one stream with a pool and a fall, closed water; edit mode with 2,2 (south) and 3,1 (east), the canon as the second input. The worker reports the first `image_gen` invocation failed in the local image loader before any generation and one successful edit followed after a lossless re-encode — one generation, honestly reported. `182,218` tokens.
- **Verdict: ACCEPTED on all ten gates** — key-light `0.0028` and rock lighting `0.038`, the flattest cell yet (the canon anchoring holds); mask completion `+987 px`, fringe `0.34% / 0.07%`; continuity closed; `139` tiles. Committed `efdbcc7`. **NINE CELLS STAND.**
- **The miss, by the worker's own checks and by eye** (`review/stitched-c2-1-cell-half.png`): "the centre reads as broadly flat moor rather than a distinct luminous gold-green capital shelf; the long station strip and the west-to-east ground line are not compositionally explicit". At half scale the cell is olive moor with a winding stream to a pool, tors and a few conifers — no raised table, no brighter meadow, no terraces bounding a shelf, no station strip. "Take every colour FROM the arriving paint" was followed to the letter and swallowed the shelf's warmer, brighter register. Crowns `45 px` again (worker); two falls instead of one.
- **What no gate measures:** whether the terrain a shelf demands is there. The gates guard lighting, water and seams; composition is the owner's eye — and the worker's self-report caught it before I did. A report-only "site present" check is not mechanisable from pixels; the brief is the lever, and the brief must make the shelf a visibly DIFFERENT ground (a raised table two shades brighter, bounded by terraces, a treeless level strip) rather than a described one.
- **Recommendation (owner's call):** replace with a `--force` bake on `brief-c2-1-r2.md` (written: the shelf as a legible bright patch at reduced zoom, bounded by low broken terraces; the station strip a treeless level meadow band three crowns wide along the shelf's south side; "brighter and warmer than the arriving paint by two steps" stated as permission, the seams still continued as they arrive). The capital is the territory's centre; it should read as a place for a city at territory zoom.
- **Evidence:** `.codex-tmp/session3/regen-c2-1.log`, `.codex-tmp/authoring/cells/c2-1/c2-1-report.json`, `review/stitched-c2-1-*`.

### R083 — 2026-09-02 — c1-3 Vendy shelf on the sound, first attempt: the brief denied a beck the paint delivered, and the shore slabs were lit (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** `brief-c1-3.md` — the sound along the south edge, an inlet reaching up to the shelf (the loop's water crossing), the Vendy shelf, closed water elsewhere, the canon as the second input; edit mode with 1,2 (north) and 2,3 (east). `136,890` tokens.
- **Verdict: REJECTED by two gates** — water continuity (`c1-2 edge: their crossing at 2992 (width 78) unmet, nearest none`) and rock lighting `0.288` (gated; the shore slabs and columns with lighter top planes and darker sides, as the worker reported). Key-light `0.0049`, fringe `0/0%`, land `80.1%`. The worker also reported 65 px of dry land on the south edge, two falls, regular terrace runs.
- **The director's error:** 1,2's beck reaches its south edge (world x `2954–3029`, 75 px wide at the line; nothing 100 px above it — the last pool's lip touches the edge). At 1,2's acceptance the continuity gate had no authored south neighbour to check, so "0 crossings" there meant nothing about the south edge; I wrote "nothing wet arrives" from the worker's "closed water" claim without reading the mask at the shared line, and the worker obeyed the brief over the edit target. **Lock-change item 5i:** the packet should list the neighbours' crossings at each shared edge as arriving water (the pipeline already computes them for the gate) — never leave that to the brief.
- **Attempt 2** (`brief-c1-3-r2.md`): the arriving beck named and required, every slab face one flat value (no lighter top, no darker side), the sound edge to edge, one fall, no two formations alike. Candidate 1 at `.codex-tmp/session3/rejected/c1-3-cand1/`.

### R084 — 2026-09-02 — c2-1 the capital's shelf, REPLACED on the stronger brief and accepted (owner: "agreed"; codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** `--force` bake on `brief-c2-1-r2.md`: the shelf as a raised table two steps brighter, bounded by broken terraces, a treeless station strip three crowns wide along its south side, ramps for the line, one fall; the canon as the second input.
- **Verdict: ACCEPTED on all ten gates** — key-light under the gate, rock lighting `0.018` on `3,771` strong edges (report-only), fringe `0/0%`, closed water, `139` tiles. Crowns `73 px` (the first attempt: `45`). Committed `b26390b`. Nine cells stand (the count is unchanged; the cell is replaced).
- **Eyes** (`review/stitched-c2-1-r2-cell-half.png`): a brighter gold-green table ringed by broken column-stub terraces, the spring pool and stream to the lower pool, conifer stands, an open strip along the south — the shelf reads as a place for a city now. The worker flags the strip narrowing in places and bright basalt tops; the owner's eye decides whether either earns another pass.
- **Model-attributable:** the first brief described the shelf; the second made it a visibly different ground with a stated permission to brighten — the landform-not-adjective lesson of R072 again.

### R085 — 2026-09-02 — The runtime LoD test: the land pyramid streamed through the app's tiers (owner: "agreed on both"; app code, dev-gated)

- **Question:** the pyramid's reduction is proven offline (R081); had the app ever streamed it? No. Would the tiers, cross-fades and residency take it?
- **Harness (all dev-gated, nothing in the release path changes):** `.codex-tmp/session3/l2dev-manifest.mjs` composes, per authored cell, a capital-tier image (the cell at L3, 256 px) and a site-tier image (the cell at L1, 1024 px) as webp under `tiles/l2-ninjaone-dev/` and writes `terrain-stream-runtime-l2dev.json` in the runtime's r4 schema — world bounds at the intended registration density (`9.45` art px per plate px; a cell = `0.0324 x 0.0576` normalized) at a dev placement `(0.38, 0.22)` east of the capital envelope (inside it the D05 city and environment plates draw over the terrain canvas and hid the first placement). `streamTiles.ts` chooses that manifest only for `NEXT_PUBLIC_LAND_STREAM=l2dev` or `?landStream=l2dev`, and allows the dev tile path beside `stream-r3/`; all other validations unchanged; `tsc` clean. A launch configuration `career-world-worktree` runs the worktree's dev server (`npm --prefix … run dev`); the first attempt served the main checkout and had to be caught by fetching the module source.
- **Measured in the app** (Browser pane, 1400x900, DPR 2.3 render scale): at span `0.37` (territory→capital) the nine cells appear as one aligned block over the old plate, the capital cohort (L3) cross-fading in; at `0.23` capital tier at `1.0`; at `0.139` the L3 images are shown at about `315` CSS px per cell (soft, as expected); at `0.0855` the site tier is at `1.0` with the nine L1 images resident (`37,748,736` decoded bytes, `9,437,184` resident pixels) — sharp, seams not findable between any two cells in the app; the old plate's inland lake, drawn by the water layer, covers the block's south-west corner as expected at this placement. **The camera's interactive floor is `0.0854` at this viewport**, so the closest view draws L1 at about 1:1 device pixels; L0 is headroom for higher device pixel ratios or a lower camera floor.
- **Verdict: the LoD works in the runtime.** Tiles land where the manifest says, the crossfade takes them, residency stays far under budget, and no tier shows a seam. What the real integration still needs, none of it a land-pipeline question: the world-scale registration (placement), a streamer with more than two source tiers if L0 is ever to be shown, and the water layer reading the published footprints instead of the old land mask.
- **Evidence:** the harness files above; screenshots in the session transcript at spans `0.37 / 0.23 / 0.139 / 0.0855`; live at `http://localhost:3000/?landStream=l2dev` while the worktree's dev server runs.

### R086 — 2026-09-02 — c1-3, second attempt: the water landed, the rock stayed lit; the lit rock is global, so the third attempt paints rock from a reference (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** `brief-c1-3-r2.md` — the arriving beck required, flat slab faces named as the measured failure, the sound edge to edge, one fall. `208,645` tokens.
- **Verdict: REJECTED by rock lighting alone** — `0.187` against `0.15` (attempt 1: `0.288`). Everything else green: continuity ok with 2 crossings (the beck continued — the brief error of R083 is closed), fringe `0/0%`, key-light `0.0026`, the sound edge to edge, one fall, the inlet as briefed. Crowns `41 px` (worker), half size again on a coast cell.
- **Localised** (`rocklight-map.mjs`, the gate's measure per 320 px block): the lit moment is `0.15–0.5` toward `30–70°` in nearly every block that has rock — not the shore, the whole cell — on only `11,085` strong rock edges (the accepted coast 4,2: `0.054` on `53,344`). So: soft, low-contrast rock with a consistent lit top wherever an edge is strong — a habit of form, not a tonal ramp, and the canon as the second input (which halved the moor's tussock emboss on 1,2) did not touch it.
- **Eyes** (`review/regen-c1-3-cand2-quarter.png`): the composition is right — the bright shelf, the beck to a pool, one fall into a narrow inlet, the sound along the whole south edge, hexagonal slab pavements at the shore. Only the rock's shading and the crown size are wrong.
- **Third attempt (`brief-c1-3-r3.md`), the experiment:** keep the composition exactly; attach a THIRD input image to the same single edit call — the accepted wild-coast cell's source (`c4-2-source.png`, rock at `0.054`) — and take the rock shading from it; a floor of 41 px on every crown. If the tool takes three images and the moment drops, that is lock-change item 5j: a per-biome reference image beside the canon (coast cells reference the accepted coast). If it refuses or the moment holds, 1,3 is parked for the owner and the lane moves to 0,3 / 4,1.
- **Evidence:** `.codex-tmp/session3/regen-c1-3.log` (after `rebake2-start`), candidate at `.codex-tmp/session3/rejected/c1-3-cand2/`.

### R087 — 2026-09-02 — c1-3, third attempt: the tool took three images and the rock stayed lit; 1,3 parked after three strikes (codex exec via cell.mjs, gpt-5.6-sol, effort=high)

- **Setup:** `brief-c1-3-r3.md` — keep the composition; the accepted coast 4,2's source attached as a THIRD input to the single edit call for the rock shading; a 41 px crown floor. The worker confirms `mode: "built-in imagegen edit with edit target, canon reference, and accepted c4-2 wild-coast rock reference"`, one call. `394,947` tokens — the heaviest run of the programme.
- **Verdict: REJECTED by rock lighting again** — `0.198` (`0.288 → 0.187 → 0.198` across the three attempts). Everything else green (continuity 2 crossings, fringe `0/0%`, key-light `0.0025`, land `77.4%`); the worker also reports sea on the east edge (94 px), a dry gap in the beck, crowns `51 px`.
- **Localised again** (`rocklight-map.mjs`): the moment is `0.1–0.5` toward `40–80°` in nearly every rock block, now on `24,494` strong edges — the lit top is the cell's habit for rock everywhere, shore and inland alike, not the slab pavements alone. The rock reference did not change it: the capital 2,1 (the same biome, the same two-image setup) passed at `0.018`; this cell, with the sound and the shelf terraces, does not, three times.
- **Ruled out by this run:** lock-change item 5j (a per-biome reference image) — a third reference image is accepted by the tool and does not move the rock-only moment here. Struck from the draft.
- **Three strikes → parked for the owner.** What a fourth attempt would change, if the owner wants one: the landform — no terraces, no column stacks, no slab pavements in this cell (a smooth shelf edge of grass over rounded rock, shingle shores, the sound over sand and low smooth ledges), so there is little strong rock edge to light; or accept the cell with rock lighting at `0.198` by the owner's eye (the crops read as bright tops on every stone at 1:1). The lane moves to 4,1 (the coast headland) meanwhile.
- **Evidence:** `.codex-tmp/session3/regen-c1-3.log` (after `rebake3-start`), candidate at `.codex-tmp/session3/rejected/c1-3-cand3/`, `review/regen-c1-3-cand3-quarter.png`, `regen-c1-3-cand3-shore-1to1.png`.

## Findings backlog (flow improvements to fold into future packets)

- F1: Task packets must declare required gate tier (focused / full / none) — otherwise workers default to maximal ceremony. (From R000.)
- F2: The 145 s camera-sweep and ~105 s package tests dominate every full-suite run; candidates for a separate `slow` lane so per-task gates stay under a minute. (Pre-existing finding from the test-suite audit, reconfirmed by R000.)
- F3: Visual-fidelity acceptance criteria must be stated numerically in the packet (coverage targets, sample-fraction thresholds, named landmarks) — "faithful to the master read" alone yields minimum-viable extraction. (From R000/T1-r1.)
- F30: The industry animates painted water by moving LIGHT, PHASE, and OVERLAYS over paint that stays put — never the paint (research dossier, `.codex-tmp/qa/T18/research/water-animation-dossier.md`). Six texture-advection rounds failed before this principle was found; the validated composite (relight + cycling + SDF swash + hash-phase twinkle over the static painting) earned the owner's first "cool." Corollaries: baked crest pixels can never translate, but overlays can (traveling overlay waves have no baked-art ceiling); future districts author their water cycling-friendly at generation; effect lanes must ship VISIBILITY PROOFS and CORRUPTION SCANS (two lanes shipped mechanically-green invisible/broken effects); perf budgets are binding gates with instrumentation. (Water program arc, 2026-08-26.)
- F29: In an axonometric world, world-parallel lines must be SCREEN-PARALLEL — generators drift toward photographic vanishing-point perspective, and wide districts with long parallel features (rail yards) make the drift glaring where small tiles hid it. Measured gate: dominant stroke directions per parallel-class within ±2° of the canonical iso axis (the owner hand-drew the exemplar). Companion: perspective consistency and alignment join per-region composition checklists — the director's glance passed what the owner's traced lines failed. (From Steve's yellow-line annotation of T22-r2, 2026-08-26.)
- F28: When concept-lock departs from the master, the LOCKED CONCEPT is the composition authority — T22-r1 scaffolded from legacy grammar footprints and generated dense old-master fabric through all-green gates; no gate measured composition. And the ECONOMICS corollary (owner-corrected framing): patch-in-place beats regeneration when the incumbent is good enough, because each regen cycle charges a defect tax (r2: perspective drift, rail misalignment) — satisficing, not sentiment. The T14 patch discipline generalizes to composition scale. (From the T22 saga, 2026-08-26.)
- F27: Calibrate every gate against the accepted baseline before locking its threshold — a gate that the currently-mounted, owner-accepted artifact fails is measuring the wrong thing, and it will discard candidates that are strictly better than what is live. The T12a coastline lock (raw ≥60% / material water-on-land ≤10% vs the T4 shore-measure silhouette) killed four candidates across three lanes before the director ran the identical measurement on the mounted plate: it scores 62.89% raw / **20.14% material — double the limit** — while discarded candidate B scores 13.59% material, strictly better than live. Even the runtime usable-mask r3 boundary only conforms 83.51%/8.06% to that silhouette. The T4 silhouette was a QA measurement artifact promoted to gate authority without ever being tested against the thing players see. Corollary to F23 (gate corrections audit retroactively) and to F17: an F17 discard is only as sound as the gate that issued it — a miscalibrated gate converts discard discipline into waste. Also from the same round: the T12b splice imported the current plate's own silhouette disagreements into the candidate (material went UP, 13.59% → 23.85%), and the QA backdrop used as splice water carried severe horizontal striping — measurement artifacts are not art assets. (Director calibration, `.codex-tmp/qa/T12b/calibrate-coast-gate.mjs`, 2026-08-25.)
- F26: One image authority per generation call — competing reference images cause drift. T11's four failed tiles were the ones briefed with exemplar crops ALONGSIDE the enriched scaffold (director packet design); its zero-drift tiles and every zero-drift result in the project's history (T7a, T9a, T9b, T10a retries) used a single complete image authority. The generator cannot serve two masters: composition exemplars, style targets, and identity notes belong in TEXT; exactly one image conditions the call. (From the T11 worker's failure analysis, 2026-08-26.)
- F25: Piecemeal tile regeneration erodes whole-plate cohesion, and network properties need both conditioning and gates — T10a's six locally-briefed re-rolls each passed local gates (drift, scale, identity) while collectively producing five circulation orphans, three malformed structures, and the owner's "drifting towards a stickered look" diagnosis (the r5 failure shape returning through per-tile optimization). Circulation is a cross-tile topological property: the T1 grammar extracted the street graph as a first-class artifact and every later stage dropped it — buildings got anchor-and-cover conditioning, roads never did. Rules: (1) uniform re-tiles from one enriched conditioning source beat defect-targeted local re-rolls — when multiple tiles need regeneration, regenerate them all from the same scaffold; (2) the scaffold carries the path network drawn in (anchor-and-cover for roads), and a continuity gate asserts border-crossing paths continue and entrances connect; (3) cohesion is judged on the stitched whole against owner-designated exemplar regions, never per-tile. Corollary to F21, from the same round: the eyes-gate needs a per-defect-class checklist — the director's S15 pass verified seating and called it fixed while never tracing the stairs to a street; "look at it" is not a gate, "answer these questions about it" is. (From Steve's round-2 review, 2026-08-25 night.)
- F24: Owner acceptance is a gate with a sampling bias, and the ledger under-recorded owner-side misses — candidate A was owner-ACCEPTED repeatedly ("overall better", coast r3) at capital framing where its 10x-dense fabric read as pleasing texture; the tiling STOP was owner-ack'd without the owner ever experiencing close zoom, then the experience was rejected the same day. Both were recorded as "owner's eyes caught X" stories (true of the catch, silent on the earlier acceptances) while director spec errors were named bluntly — asymmetric candor degrades the dataset. Rules: (1) acceptance evidence must record the framing/zoom/context it was granted under, and is only evidence for that framing; (2) owner-side misses are logged with the same fidelity as director and worker misses — the owner has explicitly endorsed this ("I'm totally fine being called out... that's the only way we grow"); (3) the symmetric director duty: an acceptance request must DELIVER the experience being accepted at the framing that matters — a live URL at the right camera, an interactive zoom, a rendered proof — never only prose and metrics about it. An ack of an undeliverable experience is weak evidence the director solicited, not an owner failure (the tiling-STOP ack: Steve ack'd numbers about a zoom feel he was never given a way to feel; he owned the handwave, but the request shape caused it). (Prompted by Steve's observation that agent-side records skew toward user satisfaction, 2026-08-25.)
- F23: When a gate is discovered blind, re-audit everything that passed on it — acceptance is only as good as the gates that granted it. The four original sprites were accepted under §7/F12 zero-matte metrics before F21 established those metrics cannot see boundary fringe; they were never re-checked under the corrected eye gate, and every subsequent packet enshrined their byte-identical hashes as protected invariants — the regression gates preserved the defect (measured: 58–70% magenta-family edge pixels on all four vs 3.74% on despilled S18; Steve spotted it live on S15). Rule: a gate correction triggers a retroactive audit of all assets that passed only the old gate; "protected/accepted" status does not exempt. Companion to F21. (From Steve's close-zoom S15 read, 2026-08-25.)
- F22: Identity gates pass beautiful miniatures — the T4b shore plate cleared coastline-conformity and all five named-identity gates while painting generic fabric at ~10x the grammar's density (~30–40 master px buildings vs the master's 90–170) and heroes ~20% under registered widths. Nothing bound painted scale to the world's registered scale, and the defect hid at capital framing for days until a close-zoom owner read (it is the r5 "stickered" failure resurfacing at the plate level). Rule: every district-plate generation packet gates (1) hero building widths within tolerance of registered display widths, (2) median generic-building width within tolerance of the grammar's footprint distribution, (3) structure count within a small factor of the grammar's count. Companion to F3/F7 (properties must encode what eyes need) and F15 (scale authority is registered data). (From Steve's post-S18-mount live read.)
- F21: The chroma metric suite (§7 matte count, F12 interior-cluster check) is structurally blind to boundary fringe — three consecutive false negatives (candidate I's spoke-gap slivers, the T8a6 2x re-read, candidate K's rim fringe), each caught only by a direct visual read of the keyed subject. Rule: every chroma-keyed asset gate ends with a 2x keyed-subject eye pass and an emitted close-up proof crop; zero-matte metrics are necessary, never sufficient. Corollary: silhouettes dominated by curved openwork maximize keyed-boundary length and are the highest-fringe-risk asset class — budget and gate them accordingly. (From T8a5–T8a7.)
- F20: SVG `userSpaceOnUse` masks are evaluated in the referencing element's LOCAL space — an element-level `transform` shifts its mask's coordinate space, so a mask whose contents already carry the same registration transform gets it applied twice (0.544² shrink + re-offset clipped the mounted D05 plate to a bottom-left fragment). Rule: position registered plates by x/y/width/height into their master rect, never by element transform, whenever a shared-space mask is attached. Focused tests passed 58/58 while this was live-broken — geometric composition defects need one live-render look (owner or director screenshot), no property test substitutes. (From Steve's live read of the fd3826f mount; fixed in f6f39b9.)
- F19: Register what the runtime consumes, not what the pipeline can measure — full geometric re-extraction (footprints, topology) was specced because it was measurable, but runtime registration is plate + district masks + named anchors (5–20 points/city, minutes of visual verification). Over-specced recovery legs add fragile machinery for data nothing consumes. (From R030.)
- F18: This generator has NO pixel-preserving edit mode — every call repaints the full frame (measured: 0/4 candidates within ROI, all 500× over tolerance). Never design a pipeline step that depends on the generator leaving pixels alone; design for whole-scene repaints and recover structure afterward (re-extraction). Companion to F11 (no alpha) — together they define the tool envelope: it paints complete pictures, nothing else. (From R029.)
- F17 (owner policy): Discard-don't-repair for generation outputs — an ROI-violating or structurally-failed candidate is regenerated, never salvaged; rework loops (worker analysis, repair passes, re-gating, repeated review) cost more than wasted calls. Deterministic post-fixes only for accepted assets with one bounded named defect. The old pipeline's months of alpha surgery on flawed assets is the cautionary precedent. (Steve, 2026-08-22.)
- F16: Verification properties must assert solidity, not existence — "at least one opaque pixel survives" passed while half of every sprite's rows were missing (scanline resampling bug). Region-level fractions (opaque coverage ≥0.98 within mapped source coverage) catch what existence checks structurally cannot. Corollary: after two same-symptom worker failures, the director sampling actual pixels in-shell (10 minutes) beats a third packet iteration. (From R022.)
- F15: Scale authority must come from registered display data, never from extracted geometry — grammar footprints measure what a classifier saw (roof cores), not what a building occupies. The layout manifest's displayWidth was the ground truth all along; every sidecar scale convention derived from extraction instead compounded a ~7× error that surfaced only at composition. Establish the world-scale reference FIRST, before any per-asset authoring. (From R017.)
- F14: Inherited registered data carries unaudited semantics — the skill roster's names came from prior agent eras and were never owner-validated, yet the contract treated them as authority and assets began baking them in. Any data that names owner-meaningful things (skills, projects, labels) gets an explicit owner audit before canonical assets consume it. (From the S18 wheelworks discovery.)
- F13: The owner-visible dashboard is itself a QA layer — Steve's console review caught a requirement-class gap (technology iconography on named buildings) that no property, packet, or director pass had encoded. Surface evidence to the owner early and continuously; a rejected group costs a re-roll, a rejected composed district costs the batch. (From R011/R013.)
- F12: Chroma-background generation can bleed the key hue into subject accents (magenta cranes) — add an explicit "metalwork never magenta/pink" prompt rule and an interior-magenta-cluster check wherever the chroma workflow is used. (From R011.)
- F31: **Python has the same execpolicy wall as Node (F5), and the unlock is different.** Headless workers cannot run the system Python: bare `python` is not on the worker PATH (`CommandNotFoundException`), and the absolute interpreter (`C:\Users\Steve\AppData\Local\Programs\Python\Python312\python.exe`) returns `Access is denied` under plain `-s workspace-write`. The bundled runtime Python (`~/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe`) is NOT the answer here as it was for Node — it ships numpy/PIL/matplotlib but is missing **moderngl and scipy**, which the ocean renderer and its diagnostics both need. **The unlock is the `--approve-for-me` flag**, which routes approvals through automatic review while staying inside the workspace-write sandbox — no `--dangerously-bypass-approvals-and-sandbox` required. Verified: a worker created a headless moderngl OpenGL 3.3 context, compiled the shaders, ran the full eikonal solve and one simulation step on the 1075x1463 plate (`CTX_OK 1075 1463`). So GPU work IS delegable. Packet template rule for any Python/GPU packet: dispatch with `--approve-for-me`, state the absolute interpreter path in the packet, and warn that the first render per state rebuilds the solve and takes minutes so it is not a hang. Discovery cost: two probes, ~20k tokens each — F4's "probe with a real workload shape before the first dispatch" paid for itself again. (From W1 probes, 2026-08-28.)
- F11: This ImageGen tool cannot emit alpha or honor canvas size — it draws a literal transparency checkerboard and picks its own resolution (reproducing the old pipeline's LFX02 failure; this is a tool property, not a prompt problem). Durable workflow: request a solid magenta background, then deterministically key alpha, decontaminate fringe, crop/rescale to the declared canvas, and measure sockets from the keyed alpha. Never ask a generator for transparency; manufacture it. (From R007.)
- F10: The headless sandbox denies all `.git` writes (index.lock creation fails) — workers can implement and verify but never commit. Standing protocol: workers finalize and report; the director verifies gates in an unsandboxed shell, stages the authorized path list, and commits. This is arguably better than the original plan — commit authority and review authority now coincide. (From R003.)
- F9: When a property's satisfiability is unknown, run a diagnostic build that *reports* the metric distribution before asserting a threshold — two consecutive blind spec corrections (r4's 60% visible, r5's 60% with inferred links) both failed against reality that one cheap measurement pass would have revealed. Measure, then spec. (From R001d.)
- F8: Spec-satisfiability is the director's burden — in occluded isometric art, visible-pixel connectivity is inherently fragmented (streets pass behind buildings/trees), so connectivity properties must admit labeled inferred links with anti-abuse bounds rather than demand what the pixels cannot show. A worker proving a property unsatisfiable is a *successful* run; count it against the spec, not the model. (From R001c.)
- F7: Numeric properties bound what workers *must* do, not what the artifact must *be* — r3 passed every stated property while producing a visually unusable graph (no connectivity requirement → confetti dashes) and fragment footprints (no roof-material requirement). Each revision should add the property that would have caught the last visual failure, and a value landing exactly on a threshold (0.7000 vs 0.70 floor) is a tuned-to-pass smell worth checking. (From R001b.)
- F6: "Audit the existing draft and finish it" instructions anchor workers to the draft's approach — attempt 4 inherited the route-splitting dead end because the packet told it to finish the draft rather than judge it. When a draft is unverified, packets should say "evaluate whether the draft's approach can satisfy the acceptance properties; discard it if not." (From R001 attempt 4.)
- F5 (supersedes earlier wording): Headless workers cannot execute the system Node at all — execpolicy allows probe-shaped calls (`node.exe --version` succeeds) but denies real execution (`node.exe -e`, `node.exe script.mjs`, `npm.cmd` → Access denied), and PATH prepends don't take effect in the worker shell. **The unlock: Codex's bundled runtime Node (`~/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe`, v24) executes scripts freely in the sandbox** — verified running the repo's sharp-based r1 builder clean. Packet template rule: all Node invocations go through the bundled binary; lint via `node.exe node_modules/eslint/bin/eslint.js`. Diagnosis method that found it: probe-matrix worker runs (~17k tokens each) comparing invocation styles, plus reading `~/.codex/rules/default.rules` for precedent (a prior interactive approval had allowlisted the bundled node for vite builds). Total discovery cost across R001 false starts: ~150k tokens. (Also: `--version` succeeding while `-e` fails means a smoke probe can false-positive an environment — probe with a *real* workload shape, not a version check.)
- F4: ~~Stale PATH CLI (v0.140.0) rejected all GPT-5.6 slugs while the desktop app's auto-updated engine (v0.149.0-alpha.4) accepted them.~~ RESOLVED 2026-08-21: Steve updated the CLI to 0.149.0; PATH `codex` now accepts 5.6 slugs and is the dispatch binary again. Durable lesson: before the first dispatch of a session, probe the model slug with a one-line read-only exec (~$0.01) — launch-gate failures are cheap to detect and expensive to discover mid-flow. Cost of this one: one dead dispatch + ~30k probe tokens. Note: Steve's config.toml sets `model_context_window=1M` for his sol default; per-dispatch `-c model_context_window=…` is available if a smaller-window model ever inherits it badly.

## Protocol

- Every `codex exec` dispatch records: packet name, model (`-c model=…`), effort (`-c model_reasoning_effort=…`), start/end time, retries/candidates used, QA verdict (pass / partial / fail / escalated), and at least one model-attributable observation (what this tier handled well or poorly).
- Same-packet reruns at a different tier get their own run IDs and a cross-reference, so tier A/Bs are queryable.
- Verdicts come from director QA against packet acceptance criteria — never from the worker's own report.

# D-014 ContextForge footprint-axis correction replan

Status: `PLAN_ONLY_REVISED_AFTER_QA_READY_FOR_DIRECTOR_REVIEW`

Run: `career-world-2026-07-16`  
Scope: `project/contextforge@v1` only  
Machine contract: `docs/career-world-production/reports/art-correction-d014-contextforge.json`

## Decision boundary

This replan creates no guide, archive, candidate, directory, raster input, generation call, promotion, runtime change, test change, queue or ledger change, architecture change, stage, or commit. A later Director packet must separately authorize execution.

D-013 is terminal `NO_GO`, but its evidence is not a systemic rejection of the one-hero contract:

- OpenAPI `PASS` proves the one-hero whole-image no-text correction can remove generated captions while preserving identity, topology, projection, palette, materials, and traceability.
- Workflow Orchestration `PASS` proves a one-hero exact-count invariant can produce the required six readable bays without a new hard-gate failure.
- ContextForge `REJECT` is an asset-specific footprint-axis failure. Its r1 hero passed integrity/identity, true-isometric projection, style/palette, no-text, physical readability, evidence boundary, and runtime isolation. It failed only primary topology and traceability because both hall banks remain collinear on one projected isometric axis. Prose saying “L-shaped” did not force a perpendicular world-axis turn.

The authoritative D-013 QA report is `docs/career-world-production/reports/art-correction-calibration-qa.json`, SHA-256 `6FF8323D2DF762CB4CBE8A53A823719BF105E0BCB5427C758D1C65689B1F82F0`.

Independent D-014 QA accepted the authority and scope boundaries but rejected two frozen vector geometries. The first retained an elbow-overlapping tower, described the cylinder on the wrong side of the L, under-specified the fallback, and ambiguously banned the named guide. The second cleared only a short ground-level witness while the same tower still occluded the complete horizontal incident return; its `z=0` witness was also 16.8 screen pixels below the actually rendered `z=0.35` plinth-top boundary. Those findings supersede the defective D-014 geometry freezes and, only where required by the occlusion proof, D-013's ContextForge tower-at-elbow placement clause. Because an opaque tower at the elbow or camera-far return edge cannot satisfy the complete visible-return invariant, the corrected guide preserves the same tower mass at the outer `+X`-leg tip and freezes the actual plinth-top return edges; this is part of the failed ContextForge footprint/elbow correction, not a broader identity or execution change.

## Immutable evidence

The following bytes remain unchanged and outside this replan’s write scope:

| Role | Path | SHA-256 |
|---|---|---|
| ContextForge canonical v0 | `design/career-world/concepts/projects/contextforge-v1.png` | `A758DC9EEFF9847D91994EA9E071BA9D54200453465D2F1D6562847B2D7D76E2` |
| ContextForge rejected-v0 archive | `design/career-world/rejected-candidates/cw004-v0/projects/contextforge-v0-a758dc9eeff9.png` | `A758DC9EEFF9847D91994EA9E071BA9D54200453465D2F1D6562847B2D7D76E2` |
| ContextForge rejected r1 candidate | `design/career-world/correction-candidates/cw004-r1/projects/contextforge-r1.png` | `1E004076E7BB85A837FD71A6B70F713D4AC13E4076C51DCA56E9F19C5FD07639` |
| OpenAPI accepted r1 candidate | `design/career-world/correction-candidates/cw004-r1/skills/openapi-r1.png` | `17383FECA2C6C5ACA13653B86919FA6351621B85FA5459C0BD758A8074B0B2C8` |
| Workflow accepted r1 candidate | `design/career-world/correction-candidates/cw004-r1/skills/workflow-orchestration-r1.png` | `0BDF4DFE5FD160A3C798AFFED243C26523C52B81DCAF4E45579CF5074DA9E7A3` |

The frozen 45-asset digest remains `2DC46F2CCEA1B4AD47480FE2254692D150AFE631840E3786F600B729C617573D`. The exact-56 canonical digest remains `D785AE233EAB09BFF84DD71D62F2085E2AA00F0683AC3A5ADFE1436A5E0FB2F3`.

## Exact supersession ruling

D-014 may supersede only these D-013 clauses, and only for ContextForge:

1. `no raster input` becomes `one deterministic guide PNG as the sole referenced image`; v0, r1, accepted peer art, tracer art, other generated art, and conversation images remain forbidden.
2. `no r2` becomes exactly one new guide-conditioned built-in imagegen call at r2; there is no retry or second call.
3. `all three calibration candidates must pass` becomes `OpenAPI PASS + Workflow PASS + ContextForge resolution`. ContextForge resolution is either a nine-gate pass by the guided r2 candidate or, after guided failure, a nine-gate pass by the deterministic vector trace-master fallback.
4. D-013's ContextForge placement clause `offset tower at the elbow` becomes `the same 1.6-by-0.7-by-5.2 tower mass at the outer +X-leg tip, x=[12.4,14.0], y=[0.35,1.05], z=[0.4,5.6], proof-disjoint from both complete rendered plinth-top returns and at least 48 painted screen pixels clear`; this supersedes tower placement only, not tower identity or the connected hall-tower-bridge-cylinder topology.

No other D-013 clause is superseded. Identity, nonfailed forms, projection, orientation, one-hero composition, style, palette, materials, evidence boundary, avoid list, easter-egg rule, reviewer independence, runtime isolation, separate promotion, and the hold on the other eight remain binding. No prompt for another asset may change.

## Preservation and planned paths

Before any guide directory or generation call, a later executor must byte-copy the rejected r1 candidate to this frozen archive path and prove equality:

`design/career-world/rejected-candidates/cw004-r1/projects/contextforge-r1-1e004076e7bb.png`

The only permitted guide evidence paths are:

- source: `design/career-world/concepts/tracer/contextforge-footprint-axis-guide-r2.svg`
- raster and immutable fallback: `design/career-world/concepts/tracer/contextforge-footprint-axis-guide-r2.png`

The only permitted guided candidate path is:

`design/career-world/correction-candidates/cw004-r2/projects/contextforge-r2-guided.png`

The archive and candidate PNG paths are covered by the current rejected/correction LFS rules. The guide PNG inherits the existing `design/career-world/concepts/**/*.png` LFS rule and must return `lfs/lfs/lfs/unset` at its exact path before creation. The source SVG remains ordinary reviewable text; it must return no LFS filter, stay UTF-8/LF deterministic, and carry exact path, byte count, SHA-256, author, and render provenance in the later execution packet. D-014 does not authorize a `.gitattributes` edit. Neither guide file counts toward or satisfies the exact-56 canonical closure or the historical tracer inventory; they require a separate two-file evidence inventory.

## Deterministic no-text vector guide

The companion JSON freezes the exact world coordinates, serialization, palette, paths, and hash procedure. The guide is one complete 1536×1024 vector hero, not a prose diagram or alternate panel:

- standard true-isometric transform `sx = 768 + s*cos(30°)*(x-y)`, `sy = 760 - s*sin(30°)*(x+y) - s*z`, with world-orthogonal `+X` and `+Y` ground axes and `+Y` projecting upper-left;
- one unmistakable concave L footprint with one leg on each different ground axis;
- the same-size tower at the outer tip of the `+X` leg at world bounds `x=[12.4,14.0]`, `y=[0.35,1.05]`, `z=[0.4,5.6]`, seated on the plinth, overlapping the hall volume, and proof-disjoint from both complete plinth-top returns;
- the `+X`-leg sawtooth bank repeats along `+X` with ridges parallel to `Y`; the `+Y`-leg bank repeats along `+Y` with ridges parallel to `X`;
- one elevated non-axis diagonal bridge from `(13.2,0.7,4.2)` to `(13.4,6.7,2.6)`, with width `0.5` and thickness `0.35`, landed inside the tower and cylinder and clear of both complete returns;
- one radius-`1.0` cylinder centered at `(13.4,6.7)`, `z=[0,2.6]`, entirely inside the open missing quadrant but outside the L footprint;
- near-black navy field, matte violet faces, thin lavender/ivory construction lines, sparse violet-cyan accent, no text or branding.

True-isometric world-perpendicular axes do **not** appear 90 degrees apart on screen. Under the frozen projection they appear as the canonical two ground directions with a 120-degree included angle (60-degree acute separation). A screen-space right angle would break true isometric. The visible proof is the concave L turn and distinct projected leg directions, not a 90-degree screen corner.

The protected geometry is the complete rendered plinth-top polyline `R_X=(14,4,0.35) -> E=(5,4,0.35) -> R_Y=(5,11,0.35)`, not a ground-level proxy. Exact projected points are `R_X=(1183.692194,311.200000)`, `E=(809.569219,527.200000)`, and `R_Y=(518.584684,359.200000)`. The former local subset `(7,4,0.35) -> E -> (5,6,0.35)` remains secondary diagnostic evidence only and cannot satisfy the gate. Exact segment-to-emitted-face/edge recomputation gives raw minima of tower `56.118446`, bridge `95.849932`, and cylinder `56.118446` pixels. With both centered 2-pixel strokes, painted gaps are `54.118446`, `93.849932`, and `54.118446` pixels, all above the 48-pixel hard gate. No x-envelope or wording-only claim is sufficient; a later guide preflight must recompute both full segments against every frozen emitted polygon edge.

The fallback is candidate-quality and fully specified before any call. The companion JSON freezes the L plinth extrusion, its fill-only top face and exactly-once boundary-stroke layer, both hall-wall volumes, every sawtooth interval and profile vertex, tower prism faces, bridge basis/vertices/faces, cylinder 24-gon projected-ellipse cap and visible side faces, palette, stroke widths/joins/caps/opacities, fixed 20-layer order, and visible-face-only policy. Hidden and dashed edges, duplicate plinth-top strokes, post-projection nudging, gradients, filters, shadows, and inferred geometry are forbidden.

The SVG may contain only the explicitly allowed deterministic inline vector elements. It may not contain `<text>`, `<title>`, `<desc>`, `<image>`, `<foreignObject>`, scripts, external references, fonts, filters, embedded raster data, logos, or metadata claims. Its rendered PNG must be exactly 1536×1024. Raw SVG and PNG hashes, renderer/version, and exact render command/settings must be computed and frozen in a later execution packet before either can be used. The guide input and fallback PNG are the same frozen byte sequence; no post-call fallback edit or second fallback raster exists. No placeholder hash permits a call.

## One-call guided contract

After preservation, LFS, geometry, render, and hash preflight, one fresh generator may make exactly one built-in imagegen call with:

- exact prompt bytes and SHA from the companion JSON;
- `referenced_image_paths` containing only the absolute resolved path of the frozen guide PNG;
- `num_last_images_to_include` omitted;
- no v0, r1, accepted peer, any other tracer art, prior generated, or conversation image; the exact named guide is the sole permitted tracer input;
- no edit retry, second generation, CLI/API fallback, compositing, paintover, or 3D fallback.

A tool error, missing output, ambiguous output path, wrong input set, prompt/hash mismatch, or unusable PNG consumes the opportunity and stops generation. The generator records receipts only and cannot review or accept the result.

## Finite fallback

If the guided candidate fails any hard gate, it is terminally rejected and no more imagegen call is permitted. The same pre-call-frozen SVG and byte-identical guide/fallback PNG then become the deterministic trace-master fallback. It is legitimate because D-010 makes deterministic code-native geometry authoritative for footprint and silhouette; generated pixels are concept references, not runtime geometry authority.

A new independent QA reviewer, distinct from the guide author, D-013 generator, D-014 generator, and guided-candidate reviewer, must run the same nine gates on the SVG/render. A `PASS` or valid `PASS_WITH_NOTES` resolves ContextForge and makes the rendered vector master promotion-eligible. A blocker leaves ContextForge terminally unresolved, keeps the other eight held, and ends this branch without another call or another fallback.

## Nine hard gates

1. `artifact_integrity_and_identity`
2. `canonical_panel_and_primary_topology`
3. `orthographic_projection_and_orientation`
4. `technical_cartography_style_and_palette_suitability`
5. `primary_form_traceability`
6. `text_logo_watermark_absence`
7. `connected_physically_readable_primary_structure`
8. `evidence_entertainment_boundary`
9. `runtime_isolation`

For gate 2, both long hall legs must be visibly non-collinear, occupy the two different true-isometric ground directions, form one visible concave L, render the complete `R_X-E-R_Y` plinth-top returns exactly once and unobscured, carry correctly rotated sawtooth banks on both legs, keep every painted tower/bridge/cylinder face or edge at least 48 screen pixels from both complete return segments, and retain the visibly diagonal non-axis bridge landed inside the tower and cylinder in the open missing quadrant. The local subset is diagnostic only. For gate 5, no whole wing, return, tower face, bridge prism, cylinder face, or connection may require invention. All other gates retain their D-013 meaning.

## Promotion and remaining-eight hold

Nothing self-promotes. A later Director promotion packet may choose exactly one passed source:

- guided r2 `PASS`: the guided candidate PNG is the promotion source and the guide remains evidence;
- guided r2 failure plus fallback `PASS`: the rendered guide PNG is the promotion source and its SVG is the trace-master source.

Promotion must independently reverify source hashes, archive preservation, exact-56 closure, frozen-45 stability, accepted OpenAPI/Workflow hashes, LFS storage, and zero runtime references/copies before replacing the canonical ContextForge PNG. Runtime code must reconstruct geometry; it may not import the guide or candidate.

The remaining eight assets stay `HOLD_UNRELEASED`. ContextForge resolution does not release them automatically; it only restores eligibility for a separate Director release under the unchanged one-call r1 contracts.

## Stop conditions

Stop without creating or mutating binaries if any frozen source, prompt, candidate, archive, digest, path, or QA result drifts; if the exact guide PNG lacks LFS coverage or the SVG is not deterministic provenance-checked text; if guide hashes/render provenance are not frozen; if the vector violates any frozen primitive, face, layer, style, visibility, projected-witness, or 48-pixel-clearance rule; if the exact prompt hash changes; if any forbidden raster is supplied; if reviewer independence fails; or if any runtime/product/test/queue/ledger/architecture/staging/commit work is bundled.

Stop after the one guided call regardless of outcome. If guided QA fails, run only the deterministic fallback QA. If fallback QA also fails, record terminal failure and do not loop.

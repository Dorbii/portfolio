export const NINJAONE_INLAND_LOCAL_ARTBOARD = Object.freeze({
  dimensions: Object.freeze([1440, 1080] as const),
  regionOrigin: Object.freeze([0.278125, 0.2] as const),
  regionSpan: Object.freeze([0.125, 1 / 6] as const),
});

export const NINJAONE_INLAND_WATER_MASK_LOCAL_CROP = Object.freeze([
  480,
  168,
  576,
  912,
] as const);
